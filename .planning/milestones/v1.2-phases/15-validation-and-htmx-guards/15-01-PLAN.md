---
phase: 15-validation-and-htmx-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fathom/forms.py
  - src/fathom/templates/partials/global_settings.html
  - tests/test_forms.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - TEST-04

must_haves:
  truths:
    - "Submitting inflation rate outside 0-20% returns a clear error message"
    - "Submitting tax rate outside 0-60% returns a clear error message"
    - "Non-numeric inflation/tax values return 'Must be a number' error"
    - "Disabling inflation/tax toggle skips validation entirely"
  artifacts:
    - path: "src/fathom/forms.py"
      provides: "validate_inflation_rate and validate_tax_rate model validators on SettingsInput"
      contains: "validate_inflation_rate"
    - path: "src/fathom/templates/partials/global_settings.html"
      provides: "HTML5 number inputs with min/max/step, disabled state, inline error display"
      contains: "aria-invalid"
    - path: "tests/test_forms.py"
      provides: "TestInflationRateValidation and TestTaxRateValidation test classes"
      contains: "TestInflationRateValidation"
  key_links:
    - from: "src/fathom/forms.py"
      to: "src/fathom/templates/partials/global_settings.html"
      via: "pydantic_errors_to_dict converting 'inflation_rate:...' prefix to 'settings.inflation_rate' key"
      pattern: "errors.*settings\\.inflation_rate"
---

<objective>
Add server-side bounds validation for inflation rate (0-20%) and tax rate (0-60%) with toggle-off bypass, plus HTML5 input hints and disabled state for toggled-off fields.

Purpose: Prevent impossible inflation/tax values from reaching the calculation engine (VAL-01, VAL-02). Ensure tests prove bounds checking works (TEST-04).
Output: Two new Pydantic model validators, updated template inputs with error display, comprehensive tests.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-validation-and-htmx-guards/15-RESEARCH.md

@src/fathom/forms.py
@src/fathom/templates/partials/global_settings.html
@tests/test_forms.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/fathom/forms.py:
```python
# Existing helper to reuse:
def _try_decimal(value: str) -> Decimal | None:

# Existing class to add validators to:
class SettingsInput(BaseModel):
    inflation_enabled: bool = False
    inflation_rate: str = "3"
    tax_enabled: bool = False
    tax_rate: str = "22"
    return_rate_preset: str = "sp500_10yr"
    return_rate_custom: str | None = None

    @model_validator(mode="after")
    def validate_return_rate(self) -> Self:
        # Pattern to follow exactly for new validators

# Error conversion:
def pydantic_errors_to_dict(exc: ValidationError) -> dict[str, str]:
    # Converts "field_name:Error message" -> {"settings.field_name": "Error message"}
```

Template error pattern (from option field templates):
```html
{% if errors.get('settings.field_name') %}aria-invalid="true"{% endif %}
<!-- ... -->
{% if errors.get('settings.field_name') %}
<small class="field-error">{{ errors['settings.field_name'] }}</small>
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inflation/tax rate validators and tests</name>
  <files>src/fathom/forms.py, tests/test_forms.py</files>
  <behavior>
    - TestInflationRateValidation:
      - test_valid_inflation_rate: SettingsInput with inflation_enabled=True, inflation_rate="3" validates without error
      - test_inflation_rate_at_bounds: inflation_rate="0" and "20" both pass
      - test_inflation_rate_below_zero: inflation_rate="-1" raises ValueError with "Inflation rate must be between 0% and 20%"
      - test_inflation_rate_above_twenty: inflation_rate="21" raises ValueError with same message
      - test_inflation_rate_non_numeric: inflation_rate="abc" raises ValueError with "Must be a number"
      - test_inflation_rate_skipped_when_disabled: inflation_enabled=False, inflation_rate="999" validates without error (toggle bypass)
    - TestTaxRateValidation:
      - test_valid_tax_rate: SettingsInput with tax_enabled=True, tax_rate="22" validates without error
      - test_tax_rate_at_bounds: tax_rate="0" and "60" both pass
      - test_tax_rate_below_zero: tax_rate="-1" raises ValueError with "Tax rate must be between 0% and 60%"
      - test_tax_rate_above_sixty: tax_rate="61" raises ValueError with same message
      - test_tax_rate_non_numeric: tax_rate="abc" raises ValueError with "Must be a number"
      - test_tax_rate_skipped_when_disabled: tax_enabled=False, tax_rate="999" validates without error
  </behavior>
  <action>
    RED: Write TestInflationRateValidation and TestTaxRateValidation test classes in tests/test_forms.py following the existing test patterns. Run tests -- they must fail because validators don't exist yet.

    GREEN: Add two model validators to SettingsInput in src/fathom/forms.py:

    1. `validate_inflation_rate`: Check `self.inflation_enabled` first -- return early if False. Then call `_try_decimal(self.inflation_rate)`. If None, raise ValueError with message `"inflation_rate:Must be a number."`. If val < 0 or val > 20, raise ValueError with `"inflation_rate:Inflation rate must be between 0% and 20%."`. Return self.

    2. `validate_tax_rate`: Same pattern with `self.tax_enabled`, `self.tax_rate`, bounds 0-60, message `"tax_rate:Tax rate must be between 0% and 60%."`.

    Follow the exact pattern of the existing `validate_return_rate` method. Use the `"field_name:Error message"` prefix format so `pydantic_errors_to_dict` correctly maps errors to template keys.

    Run tests -- they must pass.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run pytest tests/test_forms.py -x -q -k "inflation or tax_rate"</automated>
  </verify>
  <done>All inflation and tax rate validation tests pass. Valid values accepted, out-of-range rejected with specific messages, non-numeric rejected, toggle-off bypasses validation.</done>
</task>

<task type="auto">
  <name>Task 2: Update global_settings template with HTML5 hints, disabled state, and error display</name>
  <files>src/fathom/templates/partials/global_settings.html</files>
  <action>
    Update the inflation rate input in global_settings.html:
    - Change to `type="number" min="0" max="20" step="0.1"`
    - Add `{% if not settings.inflation_enabled %}disabled{% endif %}`
    - Add `{% if errors.get('settings.inflation_rate') %}aria-invalid="true"{% endif %}`
    - After the input's fieldset/group, add: `{% if errors.get('settings.inflation_rate') %}<small class="field-error">{{ errors['settings.inflation_rate'] }}</small>{% endif %}`
    - Add `onchange="document.getElementById('inflation-rate').disabled = !this.checked"` to the inflation_enabled checkbox

    Update the tax rate input similarly:
    - Change to `type="number" min="0" max="60" step="1"`
    - Add `{% if not settings.tax_enabled %}disabled{% endif %}`
    - Add `{% if errors.get('settings.tax_rate') %}aria-invalid="true"{% endif %}`
    - After the input's fieldset/group, add: `{% if errors.get('settings.tax_rate') %}<small class="field-error">{{ errors['settings.tax_rate'] }}</small>{% endif %}`
    - Add `onchange="document.getElementById('tax-rate').disabled = !this.checked"` to the tax_enabled checkbox

    Also update the custom return rate input for consistency:
    - Add `min="0" max="30" step="0.01"` (per user decision)

    Ensure the `id` attributes on inputs match what the onchange JS references. The inflation input should have `id="inflation-rate"` and tax should have `id="tax-rate"`.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run pytest tests/test_forms.py tests/test_routes.py -x -q</automated>
  </verify>
  <done>Template renders inflation/tax inputs with type="number", min/max/step attributes, disabled state when toggle is OFF, inline error display with aria-invalid, and checkbox onchange wires disabled attribute. Existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_forms.py -x -q -k "inflation or tax_rate"` -- all new validation tests pass
2. `uv run pytest -x -q` -- full test suite green
3. `uv run ruff check . && uv run ruff format --check .` -- no lint/format issues
</verification>

<success_criteria>
- SettingsInput rejects inflation rates outside 0-20% with clear error message (VAL-01)
- SettingsInput rejects tax rates outside 0-60% with clear error message (VAL-02)
- Non-numeric values get "Must be a number" error for both fields
- Disabled toggles bypass validation entirely (SC5)
- Tests prove all boundary conditions (TEST-04)
- Template shows inline errors and uses HTML5 number inputs with bounds
</success_criteria>

<output>
After completion, create `.planning/phases/15-validation-and-htmx-guards/15-01-SUMMARY.md`
</output>
