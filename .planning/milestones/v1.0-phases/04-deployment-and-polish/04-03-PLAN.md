---
phase: 04-deployment-and-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/fathom/static/style.css
  - src/fathom/templates/**
  - tests/**
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Application has no visual rough edges (spacing, alignment, typography are polished)"
    - "All existing tests pass after any code changes"
    - "Full quality toolchain passes (ruff check, ruff format, ty check, pyrefly check, prek run)"
    - "Results render within 300ms of form submission in a real browser"
    - "pyproject.toml metadata is complete (description, classifiers)"
  artifacts:
    - path: "src/fathom/static/style.css"
      provides: "Polished CSS with any rough edges fixed"
      min_lines: 1
  key_links:
    - from: "tests/"
      to: "src/fathom/"
      via: "pytest test coverage"
      pattern: "from fathom"
---

<objective>
Visual polish, code quality review, and final verification: Playwright visual audit with CSS fixes, code quality sweep, test coverage gap filling, and end-to-end performance verification.

Purpose: Ship a polished, well-tested, production-quality application. This is the final plan -- everything must be clean.
Output: CSS fixes, additional tests, verified quality across all dimensions
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-and-polish/04-01-SUMMARY.md
@.planning/phases/04-deployment-and-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual audit via Playwright and CSS polish</name>
  <files>src/fathom/static/style.css, src/fathom/templates/**</files>
  <action>
1. Start the Flask dev server (`uv run fathom` in background).
2. Use Playwright MCP to perform a visual audit:
   - Navigate to http://localhost:5000
   - Take full-page screenshot of empty form state
   - Inspect spacing, alignment, typography, color consistency, card styling
   - Fill in a sample comparison (purchase price $15000, Cash option, Traditional Loan with 5.99% APR / 36 months / $2000 down)
   - Submit the form and take screenshot of results
   - Check recommendation card, breakdown table, and chart rendering
   - Resize viewport to mobile (375px width) and take screenshot
   - Verify responsive layout stacks correctly
3. Identify and fix CSS rough edges in `src/fathom/static/style.css`:
   - Fix any spacing inconsistencies (margins, padding)
   - Fix any alignment issues (form labels, inputs, cards)
   - Ensure typography is consistent (heading sizes, font weights)
   - Verify color consistency across components
   - Check card styling on recommendation and option cards
   - Ensure charts display cleanly with no overflow
   - Per user decision: refinement within existing Pico CSS framework, not a redesign
   - Per user decision: NO dark mode, NO print-friendly CSS
4. If template HTML issues found (missing semantic elements, accessibility gaps), fix in the affected template files.
5. Re-verify with Playwright after fixes: take post-fix screenshots of both desktop and mobile views, confirm visual issues are resolved.
6. Create `tests/test_visual.py` with a Playwright-gated smoke test:
   - Use the Flask test client to start the app
   - Assert the index page returns 200 and contains key DOM elements (form, option cards, submit button)
   - Assert the results page (POST /compare with valid data) returns 200 and contains results elements (recommendation section, breakdown table, chart SVG)
   - This ensures the visual structure is verified in CI even without a browser
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . && uv run ruff format --check . && uv run pytest tests/test_visual.py -x -v && uv run pytest -x -q</automated>
  </verify>
  <done>Visual audit completed via Playwright, CSS rough edges fixed, test_visual.py validates DOM structure, application looks polished on desktop and mobile, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Code quality sweep, test gaps, and final verification</name>
  <files>tests/**, src/fathom/**</files>
  <action>
1. Run full quality toolchain and fix any issues:
   - `uv run ruff check .` -- fix all lint issues
   - `uv run ruff format .` -- format all files
   - `uv run ty check` -- fix all type issues
   - `uv run pyrefly check` -- fix all type issues
   - `uv run prek run` -- ensure all hooks pass

2. Code review sweep (fix issues directly, don't just report):
   - Security: check for hardcoded secrets, unsafe deserialization, injection vectors
   - Correctness: edge cases in form parsing, calculation edge cases
   - Code organization: any duplicated code that should be consolidated
   - Naming: unclear variable/function names
   - Dead code: unused imports, unreachable branches

3. Identify test coverage gaps and add targeted tests:
   - Check which routes/functions lack test coverage
   - Focus on untested error paths and edge cases
   - Add tests for: form validation edge cases, HTMX endpoints, option add/remove
   - Follow existing test patterns (pytest fixtures, Flask test client)
   - Ensure new tests follow per-file-ignores rules (S101, ANN, TCH excluded for tests/)

4. Final performance verification via Playwright MCP:
   - Start the server if not running
   - Navigate to http://localhost:5000
   - Fill form with typical comparison data
   - Measure time from form submission to results display
   - Assert under 300ms (PERF-01)

5. Run complete final verification:
   - `uv run pytest` -- all tests pass
   - `uv run ruff check .` -- zero lint issues
   - `uv run ruff format --check .` -- all formatted
   - `uv run prek run` -- all hooks pass
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run pytest -v && uv run ruff check . && uv run ruff format --check . && uv run prek run</automated>
  </verify>
  <done>All quality gates pass with zero issues, test coverage gaps filled, performance verified under 300ms, codebase is production-ready</done>
</task>

</tasks>

<verification>
- `uv run pytest -v` -- all tests pass (including new coverage tests and test_visual.py)
- `uv run ruff check .` -- zero lint issues
- `uv run ruff format --check .` -- all files formatted
- `uv run ty check` -- zero type errors
- `uv run pyrefly check` -- zero type errors
- `uv run prek run` -- all pre-commit hooks pass
- Playwright visual audit confirms no rough edges
- Playwright performance measurement confirms sub-300ms response
</verification>

<success_criteria>
- Visual audit completed with all CSS rough edges fixed
- Desktop and mobile layouts verified via Playwright screenshots
- test_visual.py validates DOM structure in automated verify
- Full quality toolchain passes with zero issues
- Test coverage gaps identified and filled with new tests
- Code review findings fixed (not just flagged)
- Performance verified under 300ms via both test client and Playwright
- Application is production-ready and shippable
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-and-polish/04-03-SUMMARY.md`
</output>
