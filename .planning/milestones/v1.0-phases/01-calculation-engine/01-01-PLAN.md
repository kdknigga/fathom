---
phase: 01-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/fathom/models.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_amortization.py
  - tests/test_opportunity.py
  - tests/test_inflation.py
  - tests/test_tax.py
  - tests/test_caveats.py
  - tests/test_engine.py
autonomous: true
requirements:
  - CALC-08
  - TECH-01
  - TECH-02
  - TECH-04
  - QUAL-05

must_haves:
  truths:
    - "All domain types use Decimal for monetary values, never float"
    - "Domain models capture all six option types with their distinct fields"
    - "Dual-outcome PromoResult exists for 0% promo options"
    - "Monthly data series structure exists for chart rendering in Phase 3"
    - "Caveat types are enumerated with severity levels"
    - "pytest runs and discovers test files"
    - "Test fixtures provide reusable standard options and global settings"
  artifacts:
    - path: "src/fathom/models.py"
      provides: "All domain models (inputs, outputs, enums)"
      contains: "class OptionType"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for all test modules"
      contains: "def standard_loan"
    - path: "tests/test_amortization.py"
      provides: "Failing test stubs for amortization (RED phase)"
      contains: "def test_standard_amortization"
    - path: "tests/test_engine.py"
      provides: "Failing test stubs for integration (RED phase)"
      contains: "def test_true_total_cost"
  key_links:
    - from: "tests/conftest.py"
      to: "src/fathom/models.py"
      via: "fixtures return model instances"
      pattern: "from fathom\\.models import"
    - from: "tests/test_amortization.py"
      to: "src/fathom/models.py"
      via: "test inputs use domain types"
      pattern: "FinancingOption|GlobalSettings"
---

<objective>
Set up test infrastructure, define all domain models, and create failing test stubs for the entire calculation engine.

Purpose: Establish the type contracts that all calculation modules implement against, and the test harness that verifies them. Every subsequent plan imports from models.py and runs against these tests.
Output: models.py with complete domain types, pytest configured, all test files with failing stubs.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calculation-engine/01-CONTEXT.md
@.planning/phases/01-calculation-engine/01-RESEARCH.md
@.planning/phases/01-calculation-engine/01-VALIDATION.md
@pyproject.toml
@src/fathom/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest and configure test infrastructure</name>
  <files>pyproject.toml, tests/__init__.py</files>
  <action>
Run `uv add --dev pytest` to add pytest as a dev dependency.

Add a `[tool.pytest.ini_options]` section to pyproject.toml:
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
```

Create `tests/__init__.py` as an empty file (package init).

Verify pytest discovers the tests directory by running `uv run pytest --collect-only`.
  </action>
  <verify>
    <automated>uv run pytest --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>pytest is installed as dev dependency, configured in pyproject.toml, and discovers the tests/ directory</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define domain models and create failing test stubs</name>
  <files>src/fathom/models.py, tests/conftest.py, tests/test_amortization.py, tests/test_opportunity.py, tests/test_inflation.py, tests/test_tax.py, tests/test_caveats.py, tests/test_engine.py</files>
  <behavior>
    - OptionType enum has 6 values: CASH, TRADITIONAL_LOAN, PROMO_ZERO_PERCENT, PROMO_CASH_BACK, PROMO_PRICE_REDUCTION, CUSTOM
    - CaveatType enum has 3 values: DEFERRED_INTEREST_RISK, OPPORTUNITY_COST_DOMINANCE, HIGH_INTEREST_TOTAL
    - Severity enum has values: INFO, WARNING, CRITICAL
    - FinancingOption dataclass with OptionType enum and optional fields per type (single class, not hierarchy -- per Claude's discretion and research recommendation)
    - GlobalSettings dataclass with return_rate, inflation_enabled, inflation_rate, tax_enabled, tax_rate
    - OptionResult dataclass with total_payments, total_interest, opportunity_cost, tax_savings, inflation_adjustment, rebates, true_total_cost, monthly_data
    - PromoResult dataclass with paid_on_time (OptionResult), not_paid_on_time (OptionResult), required_monthly_payment, break_even_month
    - MonthlyDataPoint dataclass with month, payment, interest_portion, principal_portion, remaining_balance, investment_balance, cumulative_cost
    - Caveat dataclass with caveat_type, message, severity
    - ComparisonResult dataclass with option_results (mapping option to result), comparison_period_months, caveats
    - All monetary fields are Decimal type
    - Test stubs import from models and assert expected behavior (will FAIL because calculation modules don't exist yet)
  </behavior>
  <action>
Create `src/fathom/models.py` with ALL domain types for the entire engine. This is the contract file -- every calculation module and every test imports from here. Use Python 3.14 dataclasses with Decimal fields. Follow the research recommendation: single FinancingOption class with OptionType enum and optional fields (not a class hierarchy).

Key model decisions per CONTEXT.md locked decisions:
- FinancingOption: option_type (OptionType), label (str), purchase_price (Decimal), apr (Decimal, optional), term_months (int, optional), down_payment (Decimal, optional), post_promo_apr (Decimal, optional for 0% promo), deferred_interest (bool, default False), retroactive_interest (bool, default True), cash_back_amount (Decimal, optional), discounted_price (Decimal, optional)
- GlobalSettings: return_rate (Decimal), inflation_enabled (bool), inflation_rate (Decimal), tax_enabled (bool), tax_rate (Decimal)
- MonthlyDataPoint: month (int), payment (Decimal), interest_portion (Decimal), principal_portion (Decimal), remaining_balance (Decimal), investment_balance (Decimal), cumulative_cost (Decimal)
- Caveat: caveat_type (CaveatType), message (str), severity (Severity)
- ComparisonResult: results (dict mapping str label to OptionResult or PromoResult), comparison_period_months (int), caveats (list[Caveat])

Create `tests/conftest.py` with shared fixtures:
- `standard_loan()` -> FinancingOption for $10,000 traditional loan, 6% APR, 36 months, no down payment
- `cash_option()` -> FinancingOption for $10,000 cash purchase
- `promo_zero_percent()` -> FinancingOption for $10,000, 0% promo, 12-month term, 24.99% post-promo APR, deferred interest on
- `default_settings()` -> GlobalSettings with 7% return, inflation off, tax off
- `settings_with_inflation()` -> GlobalSettings with 7% return, 3% inflation on, tax off
- `settings_with_tax()` -> GlobalSettings with 7% return, inflation off, 22% tax on

Create test files with RED stubs (tests that will fail because the functions they call don't exist yet). Each test file should import from the module it will test (e.g., `from fathom.amortization import monthly_payment`) and assert expected values. Use `pytest.importorskip` or guard imports so pytest can collect but tests are marked as expected failures or skipped until implementations exist. Preferred approach: use `pytest.mark.xfail(reason="not yet implemented")` on all test functions.

Test stubs to create:
- `tests/test_amortization.py`: test_standard_amortization ($10k/6%/36mo = $304.22/mo), test_zero_apr ($10k/0%/36mo = $277.78/mo), test_decimal_types (all outputs are Decimal), test_amortization_schedule (sum of payments matches expected total), test_last_payment_adjustment (final payment clears exact balance)
- `tests/test_opportunity.py`: test_cash_buyer_opportunity_cost (full price invested), test_loan_buyer_decreasing_pool (pool decreases with payments), test_pool_exhaustion (pool hits zero, stops), test_freed_cash_after_payoff (CALC-04: freed payments invested)
- `tests/test_inflation.py`: test_present_value_discounting ($304.22 at month 12, 3% inflation -> ~$295.24), test_zero_inflation (no change), test_full_term_discounting (36 months of payments)
- `tests/test_tax.py`: test_tax_savings_basic (interest * tax_rate), test_tax_disabled (returns zero), test_tax_over_full_term
- `tests/test_caveats.py`: test_deferred_interest_caveat (generated for promo options), test_opportunity_cost_dominance (sensitivity +/-2%), test_high_interest_caveat (interest > 30% of price), test_caveats_all_options (caveats for every option, not just winner)
- `tests/test_engine.py`: test_normalization_to_longest_term (CALC-03), test_true_total_cost_formula (CALC-05), test_cash_vs_loan_comparison, test_all_cash_instant_comparison, test_dual_outcome_promo

All test stubs must use `@pytest.mark.xfail(reason="not yet implemented")` so they are collected but don't block the test run.

All code must have proper docstrings (QUAL-05). Use the docstring style from research: summary on first line, blank line, extended description.

Run `uv run ruff check . && uv run ruff format --check .` after writing to confirm style compliance.
  </action>
  <verify>
    <automated>uv run pytest tests/ --collect-only -q 2>&1 | tail -5 && uv run ruff check src/fathom/models.py && uv run ruff format --check src/fathom/models.py</automated>
  </verify>
  <done>models.py defines all domain types with Decimal fields and proper docstrings. All 6 test files exist with xfail stubs. pytest collects all tests. ruff check and format pass on models.py. Fixtures in conftest.py construct valid model instances.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ --collect-only` shows all test files discovered
- `uv run pytest tests/ -v` runs with all tests xfail (no errors, no unexpected passes)
- `uv run ruff check .` passes with zero errors
- `uv run ruff format --check .` passes
- `uv run ty check` passes
- `uv run pyrefly check` passes
- `python -c "from fathom.models import OptionType, FinancingOption, GlobalSettings, OptionResult, PromoResult, MonthlyDataPoint, Caveat, ComparisonResult; print('OK')"` succeeds
</verification>

<success_criteria>
- All domain model types are importable from fathom.models
- FinancingOption uses OptionType enum to distinguish 6 option types
- All monetary fields are Decimal, never float
- pytest is configured and discovers 20+ test stubs across 6 files
- All tests are xfail (RED phase complete -- implementations come in Plan 02)
- All code quality checks pass (ruff, ty, pyrefly)
</success_criteria>

<output>
After completion, create `.planning/phases/01-calculation-engine/01-01-SUMMARY.md`
</output>
