---
phase: 02-web-layer-and-input-forms
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/fathom/routes.py
  - src/fathom/templates/index.html
  - src/fathom/templates/partials/option_card.html
  - src/fathom/templates/partials/option_list.html
  - src/fathom/templates/partials/global_settings.html
  - src/fathom/static/style.css
  - tests/test_routes.py
autonomous: false
requirements:
  - FORM-07
  - OPTY-01
  - OPTY-02
  - OPTY-03
  - OPTY-04
  - OPTY-05
  - OPTY-06
  - TECH-03

must_haves:
  truths:
    - "Full end-to-end form flow works: fill form, submit, see results placeholder with repopulated values"
    - "Type switching works for all 6 types with correct fields appearing"
    - "Reset / Start Over returns fresh default form"
    - "Mobile layout stacks single-column with sticky View Results anchor"
    - "All code passes ruff check, ruff format, ty check, pyrefly check, and prek hooks"
  artifacts:
    - path: "src/fathom/routes.py"
      provides: "All routes passing quality checks"
    - path: "src/fathom/forms.py"
      provides: "Form processing passing quality checks"
  key_links:
    - from: "src/fathom/routes.py"
      to: "src/fathom/engine.py"
      via: "compare() call on valid form submission"
      pattern: "compare\\("
---

<objective>
Quality gate enforcement and visual verification of the complete form.

Purpose: Ensure all code passes the full quality toolchain (ruff, ty, pyrefly, prek), fix any issues from Plan 01 and 02, and get human verification that the form looks and works correctly in a browser.

Output: All Phase 2 code passing quality gates, human-verified form working end-to-end.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-layer-and-input-forms/02-CONTEXT.md
@.planning/phases/02-web-layer-and-input-forms/02-01-SUMMARY.md
@.planning/phases/02-web-layer-and-input-forms/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full quality gate -- ruff, ty, pyrefly, prek, and all tests</name>
  <files>
    src/fathom/routes.py,
    src/fathom/forms.py,
    src/fathom/app.py,
    src/fathom/templates/index.html,
    src/fathom/templates/partials/option_card.html,
    src/fathom/templates/partials/option_list.html,
    src/fathom/templates/partials/global_settings.html,
    src/fathom/static/style.css,
    tests/test_routes.py,
    tests/test_forms.py
  </files>
  <action>
Run the full quality toolchain and fix ALL issues. Do not suppress errors with `# type: ignore`, `# noqa`, or by disabling rules.

1. `uv run ruff check .` -- fix all lint errors. Pay attention to:
   - D rules: all public modules, classes, and functions need docstrings
   - ANN rules: all function parameters and return types need annotations
   - S rules: security checks (S101 is already ignored in tests)
   - If ruff reports issues in new files, fix them properly

2. `uv run ruff format .` -- ensure all files formatted

3. `uv run ty check` -- fix all type errors. Common issues:
   - Flask request.form returns ImmutableMultiDict which may need type handling
   - Decimal conversions may need explicit type narrowing
   - Optional field access patterns
   - Note: tests/ is excluded from ty per pyproject.toml

4. `uv run pyrefly check` -- fix all pyrefly errors. May report different issues than ty.
   - Note: tests/ is excluded from pyrefly per pyproject.toml

5. `uv run prek run` -- run all pre-commit hooks, fix any failures

6. `uv run pytest tests/ -x -v` -- all tests pass (engine + forms + routes)

7. Review and fix any template issues:
   - Ensure all Jinja2 variable references are correct
   - Ensure HTMX attributes use correct endpoint paths
   - Ensure form field names match what parse_form_data expects
   - Ensure error key format in templates matches what validate_form_data produces

8. Verify the Reset / Start Over button works: it should be an `<a href="/">` that simply loads the GET / page with fresh defaults (FORM-07)

If any issues require changes to templates or CSS, make those fixes. The goal is zero errors across all quality tools.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check && uv run pytest tests/ -x -v && echo "ALL QUALITY GATES PASS"</automated>
  </verify>
  <done>All code passes: ruff check (zero errors), ruff format (all formatted), ty check (zero errors), pyrefly check (zero errors), prek hooks (all pass), pytest (all tests green)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of the complete form</name>
  <action>
Present the complete form to the user for visual and functional verification in the browser. The app should be started with `uv run fathom` and verified at http://127.0.0.1:5000.

What was built:
- Purchase price input card
- 2 default option cards (Cash + Traditional Loan) with editable names and type dropdowns
- Type switching via HTMX (changing dropdown swaps fields instantly)
- Add option button (up to 4 options) and remove button (down to 2)
- Global settings section (collapsed, with return rate presets, inflation toggle, tax toggle)
- Compare Options submit button and Reset / Start Over link
- Two-column desktop layout, single-column mobile
- Server-side validation with inline error display
- Form repopulation after submission

How to verify:
1. Start the app: `uv run fathom` (runs on http://127.0.0.1:5000)
2. Verify default state: two-column layout, 2 option cards, global settings collapsed with "Using defaults", buttons visible
3. Test option type switching: change dropdown to each of the 6 types, verify correct fields appear, switching clears values
4. Test add/remove: add up to 4 (button disappears), remove down to 2 (button hidden)
5. Test validation: submit with empty/invalid data, verify inline errors
6. Test form repopulation: after submission, all entered values preserved
7. Test Reset: click "Reset / Start Over" returns fresh defaults
8. Test mobile: resize browser narrow, verify single column and sticky anchor
9. Verify accessibility: all inputs have visible labels
  </action>
  <verify>User visually confirms form appearance and functionality in browser</verify>
  <done>User types "approved" confirming: form renders correctly, all interactions work (type switch, add/remove, submit, reset), layout is responsive, labels are present and consumer-friendly</done>
</task>

</tasks>

<verification>
1. All quality tools pass with zero errors
2. Human confirms form renders correctly and all interactions work
3. `uv run pytest tests/ -v` -- full test suite green
</verification>

<success_criteria>
- Zero errors across ruff check, ruff format, ty check, pyrefly check, prek hooks
- All tests pass
- Human approves form appearance and functionality in browser
- End-to-end flow works: fill form -> submit -> see results placeholder -> reset
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-layer-and-input-forms/02-03-SUMMARY.md`
</output>
