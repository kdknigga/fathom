---
phase: 02-web-layer-and-input-forms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/fathom/__init__.py
  - src/fathom/app.py
  - src/fathom/routes.py
  - src/fathom/templates/base.html
  - src/fathom/templates/index.html
  - src/fathom/templates/partials/option_card.html
  - src/fathom/templates/partials/option_list.html
  - src/fathom/templates/partials/option_fields/cash.html
  - src/fathom/templates/partials/option_fields/traditional.html
  - src/fathom/templates/partials/option_fields/promo_zero.html
  - src/fathom/templates/partials/option_fields/promo_cashback.html
  - src/fathom/templates/partials/option_fields/promo_price.html
  - src/fathom/templates/partials/option_fields/custom.html
  - src/fathom/templates/partials/global_settings.html
  - src/fathom/static/style.css
  - tests/conftest.py
autonomous: true
requirements:
  - FORM-01
  - FORM-05
  - FORM-06
  - FORM-07
  - OPTY-01
  - OPTY-02
  - OPTY-03
  - OPTY-04
  - OPTY-05
  - OPTY-06
  - A11Y-01
  - LYOT-01
  - LYOT-02
  - LYOT-03

must_haves:
  truths:
    - "GET / serves a complete HTML form page with purchase price field, 2 default option cards (Cash + Traditional Loan), and collapsed global settings"
    - "Each of the 6 option types has a distinct field template rendering correct fields for that type"
    - "Desktop renders two-column layout (form left, results placeholder right); mobile stacks single-column"
    - "All form inputs have visible labels with consumer-friendly language"
    - "Option cards have editable name field, type dropdown, and remove button in header"
  artifacts:
    - path: "src/fathom/app.py"
      provides: "Flask app factory"
      exports: ["create_app"]
    - path: "src/fathom/routes.py"
      provides: "Blueprint with GET / route"
      exports: ["bp"]
    - path: "src/fathom/templates/base.html"
      provides: "Base template with Pico CSS + HTMX CDN links"
      contains: "picocss"
    - path: "src/fathom/templates/index.html"
      provides: "Main form page with two-column grid layout"
      contains: "grid"
    - path: "src/fathom/templates/partials/option_card.html"
      provides: "Single option card with header (name, type, remove) and fields body"
      contains: "option"
    - path: "src/fathom/templates/partials/option_fields/traditional.html"
      provides: "Traditional Loan fields: APR, term, down payment"
      contains: "apr"
    - path: "src/fathom/static/style.css"
      provides: "Custom CSS for error styles, sticky anchor, minor tweaks"
  key_links:
    - from: "src/fathom/app.py"
      to: "src/fathom/routes.py"
      via: "register_blueprint"
      pattern: "register_blueprint"
    - from: "src/fathom/routes.py"
      to: "src/fathom/templates/index.html"
      via: "render_template"
      pattern: "render_template.*index"
    - from: "src/fathom/templates/index.html"
      to: "src/fathom/templates/partials/option_card.html"
      via: "Jinja2 include"
      pattern: "include.*option_card"
    - from: "src/fathom/templates/partials/option_card.html"
      to: "src/fathom/templates/partials/option_fields/*.html"
      via: "Jinja2 include with dynamic template name"
      pattern: "include.*option_fields"
---

<objective>
Set up the Flask web application and render the complete form page with all templates.

Purpose: Establish the web layer foundation -- Flask app factory, Jinja2 templates for every form section, Pico CSS styling, two-column responsive layout, and all 6 option type field templates. This gives Plan 02 a complete template layer to wire HTMX interactivity and form processing into.

Output: A running Flask app serving GET / with the full form UI rendered statically (2 default options, all field templates defined, global settings collapsed).
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-layer-and-input-forms/02-CONTEXT.md
@.planning/phases/02-web-layer-and-input-forms/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that the form must map to. -->

From src/fathom/models.py:
```python
class OptionType(Enum):
    CASH = "cash"
    TRADITIONAL_LOAN = "traditional_loan"
    PROMO_ZERO_PERCENT = "promo_zero_percent"
    PROMO_CASH_BACK = "promo_cash_back"
    PROMO_PRICE_REDUCTION = "promo_price_reduction"
    CUSTOM = "custom"

@dataclass
class FinancingOption:
    option_type: OptionType
    label: str
    purchase_price: Decimal
    apr: Decimal | None = None
    term_months: int | None = None
    down_payment: Decimal | None = None
    post_promo_apr: Decimal | None = None
    deferred_interest: bool = False
    retroactive_interest: bool = True
    cash_back_amount: Decimal | None = None
    discounted_price: Decimal | None = None

@dataclass
class GlobalSettings:
    return_rate: Decimal
    inflation_enabled: bool = False
    inflation_rate: Decimal = field(default_factory=lambda: Decimal(0))
    tax_enabled: bool = False
    tax_rate: Decimal = field(default_factory=lambda: Decimal(0))
```

From src/fathom/engine.py:
```python
def compare(options: list[FinancingOption], settings: GlobalSettings) -> ComparisonResult:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flask app factory, routes blueprint, base template, and dependency setup</name>
  <files>
    pyproject.toml,
    src/fathom/__init__.py,
    src/fathom/app.py,
    src/fathom/routes.py,
    src/fathom/templates/base.html,
    tests/conftest.py
  </files>
  <action>
1. Add Flask as a dependency: `uv add flask`. This updates pyproject.toml and uv.lock.

2. Create `src/fathom/app.py` with a `create_app()` factory function:
   - Sets `SECRET_KEY = "fathom-dev-key"`
   - Imports and registers the routes blueprint
   - Returns the Flask app instance
   - Flask(__name__) with template_folder and static_folder pointing to the correct locations relative to the package

3. Create `src/fathom/routes.py` with a Blueprint `bp`:
   - `GET /` route renders `index.html` with default form context:
     - `purchase_price=""` (empty string)
     - `options` list with 2 defaults: [{type: "cash", label: "Pay in Full", idx: 0, fields: {}}, {type: "traditional_loan", label: "Traditional Loan", idx: 1, fields: {}}]
     - `option_types` list derived from OptionType enum with human-friendly display names:
       - cash -> "Pay in Full (Cash)"
       - traditional_loan -> "Traditional Loan"
       - promo_zero_percent -> "0% Promotional Financing"
       - promo_cash_back -> "Promo with Cash-Back Rebate"
       - promo_price_reduction -> "Promo with Price Reduction"
       - custom -> "Custom / Other"
     - `settings` dict with defaults: {return_preset: "0.07", inflation_enabled: False, inflation_rate: "3", tax_enabled: False, tax_rate: "22"}
     - `errors` as empty dict
   - Import OptionType from fathom.models to drive the type dropdown (not hardcoded strings)
   - Create a module-level dict OPTION_TYPE_LABELS mapping OptionType values to display names
   - Create a module-level dict OPTION_FIELD_TEMPLATES mapping OptionType values to template filenames:
     - cash -> "partials/option_fields/cash.html"
     - traditional_loan -> "partials/option_fields/traditional.html"
     - promo_zero_percent -> "partials/option_fields/promo_zero.html"
     - promo_cash_back -> "partials/option_fields/promo_cashback.html"
     - promo_price_reduction -> "partials/option_fields/promo_price.html"
     - custom -> "partials/option_fields/custom.html"

4. Create `src/fathom/templates/base.html`:
   - `<!DOCTYPE html>` with `<html lang="en" data-theme="light">`
   - `<meta charset="utf-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1">`
   - `<title>{% block title %}Fathom - Financing Options Analyzer{% endblock %}</title>`
   - Pico CSS CDN: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">`
   - Custom CSS: `<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">`
   - HTMX CDN: `<script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>`
   - `<body>` with `<main class="container">` wrapping `{% block content %}{% endblock %}`

5. Update `src/fathom/__init__.py`: Change `main()` to create the Flask app and run it:
   ```python
   def main() -> None:
       from fathom.app import create_app
       app = create_app()
       app.run(debug=True)
   ```

6. Update `tests/conftest.py`: Add Flask test client fixtures alongside existing ones:
   - `@pytest.fixture` for `app` that calls `create_app()` and sets `TESTING = True`
   - `@pytest.fixture` for `client` that returns `app.test_client()`
   - Keep all existing conftest content intact

All modules must have module docstrings. All public functions must have docstrings. All function parameters and return types must have type annotations. Code must pass ruff check, ruff format, ty check, pyrefly check.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run python -c "from fathom.app import create_app; app = create_app(); client = app.test_client(); resp = client.get('/'); assert resp.status_code == 200; html = resp.data.decode(); assert 'picocss' in html or 'pico' in html; assert 'htmx' in html; print('OK: base app serves GET /')"</automated>
  </verify>
  <done>Flask app factory exists, GET / returns 200 with base template containing Pico CSS and HTMX CDN links, test client fixture available in conftest.py</done>
</task>

<task type="auto">
  <name>Task 2: Index page, all option field templates, global settings, and responsive layout</name>
  <files>
    src/fathom/templates/index.html,
    src/fathom/templates/partials/option_card.html,
    src/fathom/templates/partials/option_list.html,
    src/fathom/templates/partials/option_fields/cash.html,
    src/fathom/templates/partials/option_fields/traditional.html,
    src/fathom/templates/partials/option_fields/promo_zero.html,
    src/fathom/templates/partials/option_fields/promo_cashback.html,
    src/fathom/templates/partials/option_fields/promo_price.html,
    src/fathom/templates/partials/option_fields/custom.html,
    src/fathom/templates/partials/global_settings.html,
    src/fathom/static/style.css
  </files>
  <action>
1. Create `src/fathom/templates/index.html` extending `base.html`:
   - Page heading: `<h1>Fathom</h1>` with subtitle `<p>Compare the true total cost of financing options</p>`
   - Two-column Pico grid: `<div class="grid">` with two children
   - LEFT column: `<form method="post" action="/compare" id="comparison-form">`
     - Purchase Price card: `<article>` with `<header>Purchase Price</header>`, single labeled input `name="purchase_price"` with `type="text" inputmode="decimal" placeholder="e.g., 25000"`, value="{{ purchase_price }}", error display if errors.purchase_price
     - Options section: `<div id="options-container">{% include "partials/option_list.html" %}</div>`
     - Global settings: `{% include "partials/global_settings.html" %}`
     - Button row: "Compare Options" submit button (`type="submit"`) and "Reset / Start Over" link (`<a href="/" role="button" class="outline secondary">Reset / Start Over</a>`) side by side
   - RIGHT column: `<article id="results"><p>Fill in your financing options and click "Compare Options" to see results.</p></article>` (Phase 3 placeholder)
   - Mobile sticky "View Results" anchor: `<div class="sticky-results-anchor"><a href="#results">View Results</a></div>` -- visible only on mobile via CSS media query
   - All labels must use consumer-friendly language per LYOT-03: "Purchase Price", "Annual Interest Rate (APR)", "Loan Term (months)", "Down Payment", not jargon

2. Create `src/fathom/templates/partials/option_list.html`:
   - Loop: `{% for opt in options %}{% include "partials/option_card.html" %}{% endfor %}`
   - After loop, conditionally show add button: `{% if options|length < 4 %}<button type="button" hx-post="/partials/option/add" hx-target="#options-container" hx-swap="innerHTML" hx-include="#comparison-form">+ Add Financing Option</button>{% endif %}`
   - The add button uses `hx-include="#comparison-form"` to preserve form state when adding

3. Create `src/fathom/templates/partials/option_card.html`:
   - `<article id="option-{{ opt.idx }}">` (Pico CSS card)
   - `<header>` containing a Pico `.grid` with 3 items:
     - Editable name: `<input type="text" name="options[{{ opt.idx }}][label]" value="{{ opt.label }}" placeholder="Option name">`
     - Type dropdown: `<select name="options[{{ opt.idx }}][type]" hx-get="/partials/option-fields/{{ opt.idx }}" hx-target="#option-{{ opt.idx }}-fields" hx-swap="innerHTML" hx-include="this">` with options looped from `option_types`
     - Remove button: `{% if options|length > 2 %}<button type="button" class="outline secondary" hx-post="/partials/option/{{ opt.idx }}/remove" hx-target="#options-container" hx-swap="innerHTML" hx-include="#comparison-form" aria-label="Remove option">&times;</button>{% endif %}`
   - `<div id="option-{{ opt.idx }}-fields">`: Include the appropriate field template based on `opt.type` using `{% include option_template %}` where `option_template` is set by the route (pass OPTION_FIELD_TEMPLATES[opt.type] via context or use a Jinja2 macro). Simplest approach: pass `opt.template` as the template path string for each option in the context, then `{% include opt.template %}`

4. Create all 6 option field templates. Each receives `opt` (the option dict with idx and field values) and `errors` (dict). Use indexed field names `options[{{ opt.idx }}][fieldname]`:

   **cash.html**: `<p>Full purchase price paid upfront. No additional fields needed.</p>` (per locked decision)

   **traditional.html**: 3 labeled fields:
   - "Annual Interest Rate (APR)" `<input type="text" inputmode="decimal" name="options[{{ opt.idx }}][apr]" value="{{ opt.fields.apr|default('') }}" placeholder="e.g., 5.99">` with `%` suffix hint
   - "Loan Term (months)" `<input type="number" name="options[{{ opt.idx }}][term_months]" value="{{ opt.fields.term_months|default('') }}" placeholder="e.g., 36">`
   - "Down Payment" `<input type="text" inputmode="decimal" name="options[{{ opt.idx }}][down_payment]" value="{{ opt.fields.down_payment|default('') }}" placeholder="e.g., 5000">` with `$` prefix hint
   - Each field with error display: `{% if errors.get('options.' ~ opt.idx|string ~ '.apr') %}<small class="field-error">{{ errors['options.' ~ opt.idx|string ~ '.apr'] }}</small>{% endif %}`

   **promo_zero.html**: 3 fields + 1 toggle:
   - "Promotional Term (months)" -- term_months
   - "Down Payment" -- down_payment
   - "Deferred Interest" -- checkbox `name="options[{{ opt.idx }}][deferred_interest]"` with label "Interest charges apply retroactively if not paid in full"
   - "Post-Promotional APR" -- post_promo_apr (shown with hint: "Rate that applies after promotional period")

   **promo_cashback.html**: 4 fields:
   - "Annual Interest Rate (APR)" -- apr
   - "Loan Term (months)" -- term_months
   - "Cash-Back Rebate Amount" -- cash_back_amount with `$` prefix
   - "Down Payment" -- down_payment

   **promo_price.html**: 4 fields:
   - "Discounted Price" -- discounted_price with `$` prefix (hint: "Reduced price with this financing")
   - "Annual Interest Rate (APR)" -- apr
   - "Loan Term (months)" -- term_months
   - "Down Payment" -- down_payment

   **custom.html**: 4 fields:
   - "Effective APR" -- apr
   - "Term (months)" -- term_months
   - "Upfront Cash Required" -- down_payment (label changed for Custom context)
   - "Description (optional)" -- custom_label, a text input for user notes

   All fields must have: visible `<label>` with `for` attribute matching input `id`, `aria-invalid="true"` when error exists, error `<small>` below field.

5. Create `src/fathom/templates/partials/global_settings.html`:
   - `<details>` element (Pico-styled collapsible):
     - `<summary>Global Settings <small class="defaults-hint">Using defaults</small></summary>`
   - Investment Return Rate section with 3 radio buttons:
     - `<label><input type="radio" name="return_preset" value="0.04"> Conservative (4%)</label>`
     - `<label><input type="radio" name="return_preset" value="0.07" checked> Moderate (7%)</label>`
     - `<label><input type="radio" name="return_preset" value="0.10"> Aggressive (10%)</label>`
     - Manual override: `<label>Custom Rate <input type="text" inputmode="decimal" name="return_rate_custom" value="{{ settings.return_rate_custom|default('') }}" placeholder="e.g., 8.5"></label>`
   - Inflation toggle: `<label><input type="checkbox" name="inflation_enabled" value="1" {% if settings.inflation_enabled %}checked{% endif %}> Adjust for inflation</label>`
     - Conditional rate field: `<label>Inflation Rate <input type="text" inputmode="decimal" name="inflation_rate" value="{{ settings.inflation_rate|default('3') }}" placeholder="3">` with `%` suffix
   - Tax toggle: `<label><input type="checkbox" name="tax_enabled" value="1" {% if settings.tax_enabled %}checked{% endif %}> Include tax implications</label>`
     - Conditional rate field: `<label>Marginal Tax Rate <input type="text" inputmode="decimal" name="tax_rate" value="{{ settings.tax_rate|default('22') }}" placeholder="22">` with `%` suffix

6. Create `src/fathom/static/style.css` with minimal custom CSS:
   - Error styling: `.field-error { color: var(--pico-del-color); font-size: 0.85em; }` and `[aria-invalid="true"] { border-color: var(--pico-del-color); }`
   - Sticky results anchor (mobile only): `.sticky-results-anchor { display: none; } @media (max-width: 768px) { .sticky-results-anchor { display: block; position: sticky; bottom: 0; background: var(--pico-background-color); padding: 0.5rem; text-align: center; border-top: 1px solid var(--pico-muted-border-color); z-index: 10; } }`
   - Defaults hint: `.defaults-hint { color: var(--pico-muted-color); margin-left: 0.5rem; }`
   - Option card header grid alignment fixes if needed
   - Remove button sizing: keep it small and inline

7. Update `src/fathom/routes.py` GET / handler to build the full context for all templates. Each option in `options` list should be a dict with keys: `idx` (int), `type` (string, OptionType value), `label` (string), `fields` (dict of field values, empty for defaults), `template` (string, path to the field template).
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run python -c "
from fathom.app import create_app
app = create_app()
client = app.test_client()
resp = client.get('/')
html = resp.data.decode()
assert resp.status_code == 200
# Two-column grid
assert 'class=\"grid\"' in html or 'class=\\\"grid\\\"' in html, 'Missing grid layout'
# Purchase price field
assert 'purchase_price' in html, 'Missing purchase price field'
# Two default options
assert 'Pay in Full' in html, 'Missing Cash option'
assert 'Traditional Loan' in html, 'Missing Traditional Loan option'
# Option type dropdown
assert 'options[0][type]' in html or 'options[0][label]' in html, 'Missing indexed option fields'
# Global settings
assert 'Global Settings' in html, 'Missing global settings'
# Labels present
assert '<label' in html, 'Missing labels'
# Compare button
assert 'Compare Options' in html, 'Missing submit button'
# Reset button
assert 'Reset' in html or 'Start Over' in html, 'Missing reset'
# Results placeholder
assert 'results' in html.lower(), 'Missing results placeholder'
print('OK: Full form page renders with all sections')
" && uv run ruff check src/fathom/app.py src/fathom/routes.py && uv run ruff format --check src/fathom/app.py src/fathom/routes.py</automated>
  </verify>
  <done>GET / renders complete form page with: purchase price card, 2 default option cards (Cash + Traditional Loan) with editable names and type dropdowns, all 6 option field templates defined, global settings collapsed with defaults, Compare Options + Reset buttons, two-column responsive grid, mobile sticky results anchor, all inputs with visible labels, consumer-friendly language throughout</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from fathom.app import create_app; app = create_app(); print('App factory OK')"` succeeds
2. `uv run pytest tests/ -x -q` passes (existing tests still pass + new fixtures available)
3. GET / returns 200 with full form HTML containing all expected sections
4. `uv run ruff check .` passes with zero errors
5. `uv run ruff format --check .` passes
</verification>

<success_criteria>
- Flask app serves GET / with complete form UI
- All 6 option type field templates exist and render correct fields
- Two-column desktop layout present (Pico grid)
- Mobile sticky "View Results" anchor present
- All inputs have visible `<label>` elements
- Form uses consumer-friendly language (no jargon)
- Two default options (Cash + Traditional Loan) render on page load
- Global settings section collapsed with "Using defaults" hint
- Code passes ruff check and ruff format
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-layer-and-input-forms/02-01-SUMMARY.md`
</output>
