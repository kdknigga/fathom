---
phase: 12-python-linting-and-typechecking-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/fathom/routes.py
  - src/fathom/forms.py
  - tests/test_forms.py
  - tests/playwright_verify.py
autonomous: true
requirements: [LINT-01, LINT-02, LINT-03]

must_haves:
  truths:
    - "Ruff lint passes clean with PL, PT, DTZ, T20, COM812 rules enabled"
    - "All auto-fixable violations (trailing commas, if-stmt-min-max) are resolved"
    - "Test files have appropriate per-file-ignores for T201, PLC0415, PLR0915, PLR0912"
    - "All 241 existing tests still pass"
  artifacts:
    - path: "pyproject.toml"
      provides: "Expanded ruff lint configuration"
      contains: "\"PL\", \"PT\", \"DTZ\", \"T20\""
  key_links:
    - from: "pyproject.toml"
      to: "ruff check"
      via: "lint configuration"
      pattern: "select.*PL.*PT.*DTZ.*T20"
---

<objective>
Expand ruff lint rules (PL, PT, DTZ, T20, COM812), apply all auto-fixes, add per-file-ignores for test code, and fix trivial manual violations (DTZ011, PLW2901, PLR1714, PT015, PLC0415 production).

Purpose: Enable broader lint coverage across the codebase without breaking existing functionality. This is the config+auto-fix foundation that must be clean before Plan 02 refactors complex functions.
Output: pyproject.toml with expanded rules, all auto-fixable violations resolved, trivial manual fixes applied.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-python-linting-and-typechecking-cleanup/12-CONTEXT.md
@.planning/phases/12-python-linting-and-typechecking-cleanup/12-RESEARCH.md

@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand ruff config and apply auto-fixes</name>
  <files>pyproject.toml</files>
  <action>
1. In pyproject.toml `[tool.ruff.lint]` section:
   - Add "PL", "PT", "DTZ", "T20" to the `select` list
   - Add "PLR2004" to the `ignore` list (per user decision -- too noisy, 102 hits)
   - Remove "COM812" from the `ignore` list (enable trailing comma enforcement)

2. In `[tool.ruff.lint.per-file-ignores]` section:
   - Extend tests line to: `"tests/**/*.py" = ["S101", "ANN", "TCH", "T201", "PLC0415", "PLR0915", "PLR0912", "PLR0913"]`
   - Add per-file-ignore for Flask app factory lazy imports: `"src/fathom/__init__.py" = ["PLC0415"]` and `"src/fathom/app.py" = ["PLC0415"]`

3. Run `uv run ruff check --fix .` to apply all safe auto-fixes (trailing commas COM812, if-stmt-min-max PLR1730, etc.)

4. Run `uv run ruff format .` to re-format after auto-fix changes (trailing commas may cause line reflows)

5. Run `uv run ruff check .` to see remaining manual violations
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff format --check . && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pyproject.toml has expanded rules, all auto-fixable violations resolved, formatting clean, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining trivial manual violations</name>
  <files>src/fathom/routes.py, src/fathom/forms.py, tests/test_forms.py, tests/playwright_verify.py</files>
  <action>
Fix the remaining non-complexity manual violations:

1. **DTZ011 in routes.py** (~line 494): Change `date.today()` to `datetime.now(tz=timezone.utc).date()`. Add `from datetime import datetime, timezone` if not already imported.

2. **PLW2901 in forms.py**: Find the loop where a variable is reassigned (e.g., `for line in ...: line = line.strip()`). Rename the loop variable to avoid shadowing (e.g., `for raw_line in ...: line = raw_line.strip()`).

3. **PT015 in tests/test_forms.py** (3 instances): Replace `assert False, "message"` with `pytest.fail("message")`. Add `import pytest` if not present.

4. **PLR1714 in tests/playwright_verify.py** (1 instance): Replace repeated equality comparison with set membership check (e.g., `x != "a" and x != "b"` becomes `x not in {"a", "b"}`).

5. **CLI entry point check** (Claude's discretion): Read `src/fathom/__init__.py:main()`. If it uses `print()`, either add per-file-ignore for T201 on that file OR convert to `logging` -- whichever is cleaner for a CLI entry point. A CLI main() using print for startup messages is reasonable, so per-file-ignore is likely the right call.

6. Run `uv run ruff check .` to verify zero violations remain (except complexity rules PLR0911/PLR0912/PLR0915 in production code, which Plan 02 handles).

7. Run `uv run ruff format .` to ensure formatting is still clean.

8. Run full validation: `uv run pytest && uv run ty check && uv run pyrefly check`

Note: Do NOT add any `# noqa` or `# type: ignore` comments -- project policy forbids inline suppressions. Use only per-file-ignores in pyproject.toml.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . 2>&1 && uv run pytest -x -q 2>&1 | tail -5 && uv run ty check 2>&1 | tail -3 && uv run pyrefly check 2>&1 | tail -3</automated>
  </verify>
  <done>Only complexity violations (PLR0911, PLR0912, PLR0915) remain in production code -- all other violations resolved. Zero inline suppressions added. All 241+ tests pass. ty and pyrefly pass clean.</done>
</task>

</tasks>

<verification>
- `uv run ruff check .` shows only PLR0911/PLR0912/PLR0915 violations in charts.py, forms.py, formatting.py (complexity -- Plan 02)
- `uv run ruff format --check .` passes clean
- `uv run pytest` -- all tests pass
- `uv run ty check` -- passes clean
- `uv run pyrefly check` -- passes clean
- `grep -r "# noqa\|# type: ignore" src/ tests/` -- returns nothing
</verification>

<success_criteria>
Ruff config expanded with PL, PT, DTZ, T20, COM812 rules. All auto-fixable and trivial manual violations resolved. Only complexity refactoring violations remain for Plan 02. All existing tests pass. No inline suppressions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-python-linting-and-typechecking-cleanup/12-01-SUMMARY.md`
</output>
