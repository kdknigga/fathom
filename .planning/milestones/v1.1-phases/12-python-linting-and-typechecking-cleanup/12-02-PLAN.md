---
phase: 12-python-linting-and-typechecking-cleanup
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/fathom/charts.py
  - src/fathom/forms.py
  - src/fathom/formatting.py
autonomous: true
requirements: [LINT-04]

must_haves:
  truths:
    - "All production functions comply with ruff default complexity thresholds (50 statements, 10 McCabe, 12 branches, 6 returns)"
    - "Ruff check passes with zero violations across entire codebase"
    - "All 241+ existing tests still pass after refactoring"
    - "ty and pyrefly pass clean after refactoring"
  artifacts:
    - path: "src/fathom/charts.py"
      provides: "Refactored prepare_line_chart with extracted helpers"
    - path: "src/fathom/forms.py"
      provides: "Refactored validate_by_type with extracted per-type validators"
    - path: "src/fathom/formatting.py"
      provides: "Refactored comma_format with consolidated return paths"
  key_links:
    - from: "src/fathom/charts.py"
      to: "tests/test_charts.py"
      via: "pytest"
      pattern: "prepare_line_chart"
    - from: "src/fathom/forms.py"
      to: "tests/test_forms.py"
      via: "pytest"
      pattern: "validate_by_type"
---

<objective>
Refactor three complex production functions to comply with ruff's default complexity thresholds: prepare_line_chart (charts.py), validate_by_type (forms.py), and comma_format (formatting.py).

Purpose: Eliminate the remaining PLR0911/PLR0912/PLR0915 violations so ruff check passes with zero violations across the entire codebase.
Output: Refactored functions with extracted helpers, all tests passing, all lint/type tools clean.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-python-linting-and-typechecking-cleanup/12-CONTEXT.md
@.planning/phases/12-python-linting-and-typechecking-cleanup/12-RESEARCH.md
@.planning/phases/12-python-linting-and-typechecking-cleanup/12-01-SUMMARY.md

@src/fathom/charts.py
@src/fathom/forms.py
@src/fathom/formatting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor charts.py prepare_line_chart</name>
  <files>src/fathom/charts.py</files>
  <action>
Refactor `prepare_line_chart` (currently 54 statements, 15 branches) to comply with ruff defaults (50 statements, 12 branches).

Strategy: Extract dataset preparation logic into helper functions. The function likely builds multiple chart datasets in a loop with conditional logic -- extract the per-dataset building into a helper.

Approach (Claude's discretion on exact structure):
- Read the full function to understand its structure
- Identify logical sections that can be extracted (e.g., axis setup, dataset building, label formatting)
- Extract 2-3 helper functions that each handle a distinct concern
- Keep the main function as an orchestrator that calls helpers
- Ensure all extracted helpers have proper type annotations and docstrings (ruff D rules)
- Run `uv run ruff check src/fathom/charts.py` after refactoring to verify PLR0912/PLR0915 are resolved
- Run `uv run pytest tests/test_charts.py -x` to verify no regressions
- Run `uv run ty check` and `uv run pyrefly check` to verify type annotations are correct

IMPORTANT: Do NOT add `# noqa` comments. The function must actually be simplified, not suppressed.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check src/fathom/charts.py 2>&1 && uv run pytest tests/test_charts.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>prepare_line_chart has <=50 statements and <=12 branches. Ruff check on charts.py shows zero violations. All chart tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor forms.py validate_by_type and formatting.py comma_format</name>
  <files>src/fathom/forms.py, src/fathom/formatting.py</files>
  <action>
Two refactoring targets:

**1. forms.py `validate_by_type`** (62 statements, 26 branches):
- This is the most complex function in the codebase
- Strategy: Extract per-type validation into separate functions (e.g., `_validate_decimal`, `_validate_integer`, `_validate_percentage`, etc.)
- The main function should dispatch to type-specific validators, likely via a dict mapping or if/elif chain that calls helpers
- Each helper handles one field type's validation logic
- Ensure all extracted helpers have proper type annotations and docstrings
- Run `uv run ruff check src/fathom/forms.py` to verify PLR0912/PLR0915 resolved
- Run `uv run pytest tests/test_forms.py -x` to verify no regressions

**2. formatting.py `comma_format`** (8 return statements, limit 6):
- Strategy: Consolidate early-exit return paths
- Options: combine related conditions, use match/case, or restructure to reduce exit points
- This is a simpler refactor -- likely just combining a couple of guard clauses
- Run `uv run ruff check src/fathom/formatting.py` to verify PLR0911 resolved

**Final validation:**
- `uv run ruff check .` -- zero violations across entire codebase
- `uv run ruff format --check .` -- formatting clean
- `uv run pytest` -- all tests pass
- `uv run ty check` -- clean
- `uv run pyrefly check` -- clean
- `grep -r "# noqa\|# type: ignore" src/ tests/` -- returns nothing

IMPORTANT: Do NOT add `# noqa` comments. Functions must actually be simplified.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . 2>&1 && uv run pytest -x -q 2>&1 | tail -5 && uv run ty check 2>&1 | tail -3 && uv run pyrefly check 2>&1 | tail -3</automated>
  </verify>
  <done>validate_by_type has <=50 statements and <=12 branches. comma_format has <=6 return statements. Ruff check across entire codebase shows zero violations. All tests pass. ty and pyrefly pass clean. No inline suppressions anywhere.</done>
</task>

</tasks>

<verification>
- `uv run ruff check .` -- zero violations (entire codebase)
- `uv run ruff format --check .` -- clean
- `uv run pytest` -- all 241+ tests pass
- `uv run ty check` -- clean
- `uv run pyrefly check` -- clean
- `grep -r "# noqa\|# type: ignore" src/ tests/` -- returns nothing (no inline suppressions)
- `uv run prek run` -- all pre-commit hooks pass
</verification>

<success_criteria>
All three complex functions refactored below ruff default thresholds. Zero ruff violations across entire codebase. All existing tests pass. All type checkers pass clean. No inline suppressions added.
</success_criteria>

<output>
After completion, create `.planning/phases/12-python-linting-and-typechecking-cleanup/12-02-SUMMARY.md`
</output>
