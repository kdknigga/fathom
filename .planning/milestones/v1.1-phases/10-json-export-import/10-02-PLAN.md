---
phase: 10-json-export-import
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/playwright_verify.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Export button triggers a file download, not a DOM swap"
    - "Import file picker opens and form auto-submits on file selection"
    - "Imported values appear correctly in form fields"
    - "Invalid import shows inline error messages in the browser"
  artifacts:
    - path: "tests/playwright_verify.py"
      provides: "Playwright browser verification for export/import"
      contains: "export"
  key_links:
    - from: "tests/playwright_verify.py"
      to: "src/fathom/templates/index.html"
      via: "Playwright DOM assertions"
      pattern: "export|import"
---

<objective>
Verify export/import functionality in a real browser using Playwright MCP.

Purpose: Per CLAUDE.md, all browser-based verification must be automated via Playwright MCP. This confirms the export button triggers a file download (not HTMX DOM swap), the import file picker works, imported values populate fields, and error messages render correctly.

Output: Playwright verification confirming end-to-end browser behavior.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-json-export-import/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright browser verification of export/import</name>
  <files>tests/playwright_verify.py</files>
  <action>
    Use Playwright MCP to verify the following in a real browser (start the app with `uv run fathom` first if not already running):

    1. **Export button exists and is styled correctly:**
       - Navigate to http://localhost:5000
       - Verify Export button is visible with "outline" styling
       - Verify Export button has formaction="/export" attribute

    2. **Import button exists:**
       - Verify Import label/button is visible
       - Verify hidden file input exists with accept=".json"

    3. **Export triggers download (not DOM swap):**
       - Fill in form fields with test values (purchase price, option labels)
       - Click Export button
       - Verify a file download is triggered (check for download event or Content-Disposition response)
       - If HTMX intercepts and shows JSON in page instead of downloading, document the issue

    4. **Import restores form values:**
       - Create a valid test JSON file
       - Upload it via the import file input
       - Verify form fields are populated with the imported values
       - Verify no error messages are shown

    5. **Invalid import shows error:**
       - Upload a file with invalid JSON content
       - Verify structural error message is displayed

    6. **Accessibility:**
       - Verify Export button is keyboard-focusable
       - Verify Import label is keyboard-accessible (role="button")

    Add any new Playwright checks to tests/playwright_verify.py alongside the existing verification functions. Follow the existing pattern in that file.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run pytest tests/test_routes.py -x -q</automated>
  </verify>
  <done>Playwright verification confirms: Export downloads a file (not DOM swap), Import populates form fields, invalid import shows error message, buttons are keyboard accessible.</done>
</task>

</tasks>

<verification>
- Playwright MCP screenshots confirm button placement and styling
- Export produces file download in browser
- Import round-trip works end-to-end in browser
- Error states render correctly
- Full test suite still passes: `uv run pytest -x -q`
</verification>

<success_criteria>
- Export button triggers browser file download with correct filename
- Import file picker accepts .json files and auto-submits
- Imported values visible in all form fields after upload
- Invalid file upload shows user-friendly error message
- No HTMX interference with export/import flows
</success_criteria>

<output>
After completion, create `.planning/phases/10-json-export-import/10-02-SUMMARY.md`
</output>
