---
phase: 09-tooltips-and-tax-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fathom/tax_brackets.py
  - src/fathom/routes.py
  - src/fathom/results.py
  - src/fathom/static/style.css
  - src/fathom/static/tooltips.js
autonomous: true
requirements:
  - TAX-01
  - TAX-02
  - TIPS-02
  - TIPS-03

must_haves:
  truths:
    - "Tax bracket data loads with 7 correct 2025 IRS brackets"
    - "Breakdown table rows carry tooltip text for each metric"
    - "CSS styles for .tip, .tooltip-content, .bracket-table, and dark mode variants exist"
    - "tooltips.js handles bracket row click-to-fill and keyboard activation"
  artifacts:
    - path: "src/fathom/tax_brackets.py"
      provides: "2025 IRS tax bracket data constant"
      contains: "TAX_BRACKETS_2025"
    - path: "src/fathom/static/style.css"
      provides: "Tooltip button, popover, and tax bracket CSS with dark mode"
      contains: ".tip"
    - path: "src/fathom/static/tooltips.js"
      provides: "Tax bracket row click handler"
      contains: "bracket-row"
  key_links:
    - from: "src/fathom/routes.py"
      to: "src/fathom/tax_brackets.py"
      via: "import and pass tax_brackets to template context"
      pattern: "tax_brackets"
    - from: "src/fathom/results.py"
      to: "breakdown_table.html"
      via: "tooltip field added to breakdown row dicts"
      pattern: "tooltip"
    - from: "src/fathom/static/tooltips.js"
      to: "src/fathom/templates/partials/global_settings.html"
      via: "getElementById targeting tax-rate input (confirmed ID exists)"
      pattern: "tax-rate"
---

<objective>
Create the backend data, CSS infrastructure, and JS handler for tooltips and tax bracket widget.

Purpose: Establish the foundation (Python data, styles, scripts) that template modifications in 09-02 will consume.
Output: Tax bracket constant, tooltip text in breakdown rows, complete CSS for tips/popovers/brackets, tooltips.js with bracket click handler.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tooltips-and-tax-guidance/09-CONTEXT.md
@.planning/phases/09-tooltips-and-tax-guidance/09-RESEARCH.md

<interfaces>
<!-- Key existing structures the executor needs -->

From src/fathom/results.py (breakdown components):
```python
_BREAKDOWN_COMPONENTS: list[tuple[str, str]] = [
    ("Total Payments", "total_payments"),
    ("Total Interest", "total_interest"),
    ("Rebates", "rebates"),
    ("Opportunity Cost", "opportunity_cost"),
    ("Tax Savings", "tax_savings"),
    ("Inflation Adjustment", "inflation_adjustment"),
    ("True Total Cost", "true_total_cost"),
]
```

From src/fathom/results.py (_build_breakdown_rows returns):
```python
# Each row: {"label": str, "values": list[dict]}
# Values have: {"value": Decimal, "is_promo": bool} or {"paid": Decimal, "not_paid": Decimal, "is_promo": True}
```

From src/fathom/routes.py (index route renders with):
```python
return render_template("index.html",
    purchase_price="",
    options=_build_default_options(),
    option_types=_build_option_types(),
    settings={...},
    errors={},
)
```

From src/fathom/static/formatting.js (event delegation pattern):
```javascript
var form = document.getElementById("comparison-form");
form.addEventListener("click", function (e) { ... });
```

From src/fathom/templates/partials/global_settings.html (confirmed IDs):
```html
<!-- tax rate input ID: "tax-rate" -->
<!-- tax enabled checkbox name: "tax_enabled" -->
<label for="tax-rate">Marginal Tax Rate</label>
<input type="text" id="tax-rate" name="tax_rate" ...>
<input type="checkbox" name="tax_enabled" ...>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tax bracket data module and route wiring</name>
  <files>src/fathom/tax_brackets.py, src/fathom/routes.py, src/fathom/results.py</files>
  <action>
1. Create `src/fathom/tax_brackets.py` with:
   - Module docstring citing IRS Rev. Proc. 2024-40 as source.
   - `TAX_BRACKETS_2025` constant: list of dicts with keys `rate` (int), `single_min` (int), `single_max` (int|None), `mfj_min` (int), `mfj_max` (int|None). Use the verified 2025 IRS data from RESEARCH.md (7 brackets, 10% through 37%).
   - Do NOT add a format_bracket_range helper or pre-format range strings. Per user decision, income range formatting uses the existing `|comma` Jinja filter directly in templates.

2. Update `src/fathom/routes.py`:
   - Import `TAX_BRACKETS_2025` from `fathom.tax_brackets`.
   - In the `index()` route, add `tax_brackets=TAX_BRACKETS_2025` to the `render_template` call.
   - In the `compare_options()` route, also pass `tax_brackets=TAX_BRACKETS_2025` to the results template render (so the tax bracket widget persists after form submission).

3. Update `src/fathom/results.py`:
   - Create a `_TOOLTIP_TEXT` dict mapping breakdown row labels to tooltip explanation strings. Each tooltip: 2-3 sentences covering definition, why it matters, and a concrete example. Labels: "Total Payments", "Total Interest", "Rebates", "Opportunity Cost", "Tax Savings", "Inflation Adjustment", "True Total Cost".
   - In `_build_breakdown_rows`, add a `"tooltip"` key to each row dict: `rows.append({"label": label, "values": values, "tooltip": _TOOLTIP_TEXT.get(label, "")})`.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . && uv run ruff format --check . && uv run python -c "
from fathom.tax_brackets import TAX_BRACKETS_2025
assert len(TAX_BRACKETS_2025) == 7, f'Expected 7 brackets, got {len(TAX_BRACKETS_2025)}'
assert TAX_BRACKETS_2025[0]['rate'] == 10
assert TAX_BRACKETS_2025[0]['single_max'] == 11_925
assert TAX_BRACKETS_2025[1]['single_min'] == 11_926
assert TAX_BRACKETS_2025[1]['single_max'] == 48_475
assert TAX_BRACKETS_2025[3]['mfj_max'] == 394_600
assert TAX_BRACKETS_2025[6]['rate'] == 37
assert TAX_BRACKETS_2025[6]['single_max'] is None
print('Bracket data verified OK')
" && uv run python -c "
from fathom.results import _build_breakdown_rows
print('Results import OK')
" && uv run pytest tests/test_routes.py -x -q</automated>
  </verify>
  <done>Tax bracket data importable with 7 brackets and verified IRS values. Routes pass tax_brackets to templates. Breakdown rows include tooltip text. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CSS styles and tooltips.js</name>
  <files>src/fathom/static/style.css, src/fathom/static/tooltips.js</files>
  <action>
1. Append to `src/fathom/static/style.css`:
   - CSS custom properties in `:root` for tip and tooltip colors: `--tip-border`, `--tip-color`, `--tip-bg`, `--tooltip-bg`, `--tooltip-border`, `--tooltip-text`.
   - `.tip` button styles: `all: unset`, inline-flex circle ~16px, border-radius 50%, 0.7rem bold font, border 1.5px solid using `--tip-border`, color `--tip-color`, background `--tip-bg`, cursor pointer, vertical-align middle, margin-left 0.25rem.
   - `.tip:focus-visible` with 2px outline using `--pico-primary`, offset 2px.
   - `.tooltip-content` popover styles: max-width 320px, padding 0.75rem 1rem, border-radius, border, background, color, font-size 0.875rem, line-height 1.5, box-shadow.
   - `.label-with-tip` flex container: display flex, align-items baseline, gap 0.25rem.
   - CSS Anchor Positioning with `@supports (anchor-name: --tip)` -- use implicit invoker-anchor relationship so `.tooltip-content` gets `position: absolute; position-area: top center; position-try-fallbacks: bottom center, right center, left center; margin: 0; inset: auto;`.
   - Fallback with `@supports not (anchor-name: --tip)` -- `.tooltip-content` gets `position: fixed; top: 2rem; left: 50%; transform: translateX(-50%);`.
   - `.tax-bracket-ref` styles for the details element.
   - `.bracket-table` styles: full-width table, proper padding, row hover highlight.
   - `.bracket-row` styles: cursor pointer, hover background highlight, focus-visible outline.
   - `.bracket-row[aria-selected="true"]` selected state.
   - Dark mode variants inside `@media (prefers-color-scheme: dark) { :root:not([data-theme]) { ... } }` following the established pattern -- tooltip-bg dark (#1e293b), tooltip-border (#475569), tooltip-text (#e2e8f0).

2. Create `src/fathom/static/tooltips.js`:
   - IIFE wrapping, "use strict", same pattern as formatting.js.
   - Get `#comparison-form` element; return early if not found.
   - NOTE: The tax rate input ID is confirmed as `tax-rate` (from global_settings.html line 86). The tax enabled checkbox name is `tax_enabled`.
   - Delegated click handler on form: if `e.target.closest(".bracket-row")`, get `data-rate`, set `document.getElementById("tax-rate")` input value, check `[name="tax_enabled"]` checkbox if unchecked, add `aria-selected="true"` to clicked row and remove from siblings.
   - Delegated keydown handler on form: if Enter or Space on `.bracket-row`, `e.preventDefault()`, trigger `row.click()`.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . && uv run ruff format --check . && test -f src/fathom/static/tooltips.js && grep -q 'tax-rate' src/fathom/static/tooltips.js && grep -q 'bracket-row' src/fathom/static/tooltips.js && grep -q '.tip' src/fathom/static/style.css && grep -q '.tooltip-content' src/fathom/static/style.css && grep -q '.bracket-table' src/fathom/static/style.css && echo "CSS and JS verified OK"</automated>
  </verify>
  <done>CSS styles for .tip, .tooltip-content, .label-with-tip, .bracket-table, .bracket-row exist with dark mode variants. tooltips.js handles bracket row click (targeting confirmed #tax-rate ID) and keyboard activation.</done>
</task>

</tasks>

<verification>
- `uv run ruff check . && uv run ruff format --check .` -- zero lint/format errors
- `uv run pytest tests/ -x -q` -- all existing tests pass
- `uv run python -c "from fathom.tax_brackets import TAX_BRACKETS_2025; assert len(TAX_BRACKETS_2025) == 7"` -- bracket data loads
- Bracket values spot-checked against 2025 IRS data (10% single max = $11,925; 12% single range $11,926-$48,475; 37% bracket has None max)
</verification>

<success_criteria>
- Tax bracket data module exists with correct 2025 IRS values for all 7 brackets (TAX-02)
- Routes pass tax_brackets to template context (TAX-01 foundation)
- Breakdown rows include tooltip text for all metrics (TIPS-02 foundation)
- CSS for tips, popovers, bracket table, and dark mode complete (TIPS-03 foundation)
- tooltips.js handles bracket row click and keyboard activation targeting confirmed #tax-rate input (TAX-01 foundation)
- Zero new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/09-tooltips-and-tax-guidance/09-01-SUMMARY.md`
</output>
