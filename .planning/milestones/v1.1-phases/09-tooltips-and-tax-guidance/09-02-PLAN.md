---
phase: 09-tooltips-and-tax-guidance
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/fathom/templates/index.html
  - src/fathom/templates/partials/global_settings.html
  - src/fathom/templates/partials/option_fields/traditional.html
  - src/fathom/templates/partials/option_fields/promo_zero.html
  - src/fathom/templates/partials/option_fields/promo_cashback.html
  - src/fathom/templates/partials/option_fields/promo_price.html
  - src/fathom/templates/partials/option_fields/custom.html
  - src/fathom/templates/partials/option_fields/cash.html
  - src/fathom/templates/partials/results/breakdown_table.html
  - src/fathom/templates/partials/results/recommendation.html
autonomous: true
requirements:
  - TIPS-01
  - TIPS-02
  - TIPS-03
  - TAX-01
  - TAX-02

must_haves:
  truths:
    - "User can click a ? icon next to any form field and see a plain-English explanation in a popover"
    - "User can click a ? icon next to any result metric and see an explanation"
    - "All tooltips are keyboard-focusable, dismissable with Escape, and hoverable without disappearing"
    - "User can expand a tax bracket reference near the tax rate field showing 2025 IRS brackets"
    - "Clicking a tax bracket row auto-fills the tax rate input with that rate"
    - "Income ranges in the bracket table are formatted with commas using the |comma Jinja filter"
  artifacts:
    - path: "src/fathom/templates/partials/global_settings.html"
      provides: "Tax bracket details widget below tax rate field"
      contains: "tax-bracket-ref"
    - path: "src/fathom/templates/partials/results/breakdown_table.html"
      provides: "Tooltip buttons on each breakdown row label"
      contains: "popovertarget"
    - path: "src/fathom/templates/partials/results/recommendation.html"
      provides: "Tooltip button next to True Total Cost"
      contains: "tip-rec-true-total-cost"
  key_links:
    - from: "templates (all)"
      to: "style.css"
      via: "popover attribute + .tip class + .tooltip-content class"
      pattern: "popovertarget"
    - from: "global_settings.html"
      to: "tax_brackets (template context)"
      via: "Jinja loop over tax_brackets with |comma filter for ranges"
      pattern: "bracket.single_min|comma"
---

<objective>
Add tooltip ? buttons to all form field labels and result metric labels, plus the tax bracket reference widget.

Purpose: Users see plain-English explanations for every financial term via native HTML popover API. Tax bracket widget provides income range reference with click-to-fill.
Output: All 10 template files updated with tooltip buttons, popover content, and tax bracket widget.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tooltips-and-tax-guidance/09-CONTEXT.md
@.planning/phases/09-tooltips-and-tax-guidance/09-RESEARCH.md
@.planning/phases/09-tooltips-and-tax-guidance/09-01-SUMMARY.md

<interfaces>
<!-- From 09-01: tax_brackets context variable structure -->
<!-- Each bracket dict has: rate (int), single_min (int), single_max (int|None), mfj_min (int), mfj_max (int|None) -->
<!-- Use |comma Jinja filter for formatting: {{ bracket.single_min|comma }} -->
<!-- DO NOT pre-format ranges in Python -- use |comma filter in templates per user decision -->

From src/fathom/templates/partials/option_fields/traditional.html (label pattern):
```html
<label for="option-{{ opt.idx }}-apr">Annual Interest Rate (APR)</label>
<fieldset role="group">
  <input type="text" inputmode="decimal" id="option-{{ opt.idx }}-apr" ...>
```

From src/fathom/templates/partials/results/breakdown_table.html:
```html
{% for row in display.breakdown_rows %}
<tr>
  <td class="row-label">{{ row.label }}</td>
  ...
</tr>
{% endfor %}
```

Tooltip pattern (button OUTSIDE label to avoid click-forwarding -- Pitfall 2):
```html
<div class="label-with-tip">
  <label for="option-{{ opt.idx }}-apr">Annual Interest Rate (APR)</label>
  <button type="button" class="tip" popovertarget="tip-apr-{{ opt.idx }}" aria-label="What is APR?">?</button>
</div>
<div id="tip-apr-{{ opt.idx }}" popover class="tooltip-content">
  <p>...</p>
</div>
```

Tax bracket table pattern (uses |comma filter, NOT pre-formatted strings):
```html
<td>${{ bracket.single_min|comma }}{% if bracket.single_max %} - ${{ bracket.single_max|comma }}{% else %}+{% endif %}</td>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tooltips to global settings, index.html, and tax bracket widget</name>
  <files>src/fathom/templates/index.html, src/fathom/templates/partials/global_settings.html</files>
  <action>
1. `index.html`:
   - Wrap the `<label for="purchase-price">` in a `.label-with-tip` div with ? button and popover. Tooltip content: "The full retail price of the item before any financing, discounts, or down payments. This is the base amount that all financing options are compared against."
   - Add `<script src="{{ url_for('static', filename='tooltips.js') }}"></script>` tag after the formatting.js script tag.

2. `global_settings.html`:
   - Investment Return Rate: Add ? button after the legend/label text with popover explaining opportunity cost rate concept ("The annual return you could earn by investing your money instead of paying upfront. For example, if you could earn 7% in a stock index fund, that's the opportunity cost of paying cash.").
   - Inflation Rate label: Add ? button and popover ("Inflation reduces the real value of future payments. At 3% inflation, a $100 payment next year only costs you about $97 in today's dollars. Higher inflation makes longer loan terms slightly cheaper in real terms.").
   - Marginal Tax Rate label: Add ? button and popover ("Your marginal tax rate is the rate on your last dollar of income -- not your average rate. If your loan interest is tax-deductible, this rate determines how much you save. A 24% marginal rate means each $1,000 in deductible interest saves $240 in taxes.").
   - AFTER the tax rate fieldset (but still inside the tax `<fieldset>`), add the tax bracket `<details>` widget:
     ```html
     <details class="tax-bracket-ref">
       <summary>What's my bracket?</summary>
       <table class="bracket-table">
         <thead>
           <tr>
             <th scope="col">Rate</th>
             <th scope="col">Single</th>
             <th scope="col">Married Filing Jointly</th>
           </tr>
         </thead>
         <tbody>
           {% for bracket in tax_brackets %}
           <tr class="bracket-row" data-rate="{{ bracket.rate }}" tabindex="0"
               role="button" aria-label="Select {{ bracket.rate }}% tax rate">
             <td>{{ bracket.rate }}%</td>
             <td>${{ bracket.single_min|comma }}{% if bracket.single_max %} - ${{ bracket.single_max|comma }}{% else %}+{% endif %}</td>
             <td>${{ bracket.mfj_min|comma }}{% if bracket.mfj_max %} - ${{ bracket.mfj_max|comma }}{% else %}+{% endif %}</td>
           </tr>
           {% endfor %}
         </tbody>
       </table>
       <small>Click a row to use that rate. Source: IRS Rev. Proc. 2024-40 (2025 tax year)</small>
     </details>
     ```
   - CRITICAL: Use the `|comma` Jinja filter for income range formatting. Do NOT pre-format ranges in Python. The bracket dicts have raw integer values (single_min, single_max, mfj_min, mfj_max) and the template formats them inline.

   IMPORTANT: Every popover `id` must be globally unique. Use static names for global settings (e.g., `tip-return-rate`, `tip-inflation-rate`, `tip-tax-rate`, `tip-purchase-price`). Place button OUTSIDE `<label>` tag.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run python -c "
from fathom import create_app
app = create_app()
client = app.test_client()
resp = client.get('/')
assert resp.status_code == 200, f'Index returned {resp.status_code}'
html = resp.data.decode()
assert 'tax-bracket-ref' in html, 'Tax bracket widget missing'
assert 'bracket-row' in html, 'Bracket rows missing'
assert 'popovertarget' in html, 'Tooltip buttons missing'
assert 'tooltips.js' in html, 'tooltips.js script tag missing'
assert '|comma' not in html, 'Raw Jinja filter leaked into rendered HTML'
assert '11,925' in html, 'Comma-formatted bracket value missing'
print('Global settings + index templates verified OK')
"</automated>
  </verify>
  <done>Purchase price, investment return rate, inflation rate, and tax rate fields have ? tooltip buttons. Tax bracket widget renders below tax rate with 7 rows using |comma filter for income ranges. tooltips.js script tag added to index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Add tooltips to all option field templates and result templates</name>
  <files>src/fathom/templates/partials/option_fields/traditional.html, src/fathom/templates/partials/option_fields/promo_zero.html, src/fathom/templates/partials/option_fields/promo_cashback.html, src/fathom/templates/partials/option_fields/promo_price.html, src/fathom/templates/partials/option_fields/custom.html, src/fathom/templates/partials/option_fields/cash.html, src/fathom/templates/partials/results/breakdown_table.html, src/fathom/templates/partials/results/recommendation.html</files>
  <action>
For every form field label in option templates, wrap in a `.label-with-tip` div with a `<button>` trigger and a `<div popover>` element. CRITICAL: Place button OUTSIDE the `<label>` tag to avoid click-forwarding issues (Pitfall 2 from research).

IMPORTANT: Every popover `id` must be globally unique. Use field-name + option index for option fields (e.g., `tip-apr-{{ opt.idx }}`).

**Option field templates:**

1. `traditional.html` -- 3 fields: APR, Loan Term, Down Payment. Add ? button + popover to each label.
   - APR tooltip: "The Annual Percentage Rate is the yearly interest rate on your outstanding loan balance. A $10,000 loan at 6% APR costs about $600 in interest the first year, decreasing as you pay down the balance."
   - Loan Term tooltip: "The number of months to fully repay the loan. A longer term means lower monthly payments but more total interest. For example, a $10,000 loan at 6% costs $580 in interest over 24 months vs. $1,200 over 48 months."
   - Down Payment tooltip: "Cash paid upfront at the time of purchase, reducing the amount you need to finance. A $2,000 down payment on a $10,000 purchase means you only finance $8,000."

2. `promo_zero.html` -- 4+ fields: Promotional Term, Down Payment, Deferred Interest checkbox, Post-Promo APR.
   - Promotional Term tooltip: "The number of months with 0% interest. You pay no interest during this period, but if you don't pay the balance in full before it ends, different rules may apply depending on whether interest is deferred."
   - Down Payment tooltip: Same as traditional.
   - Deferred Interest tooltip: "If checked, interest accrues silently from the purchase date at the Post-Promo APR. If you pay in full before the promo ends, you owe nothing extra. If any balance remains, you owe ALL the accrued interest at once -- this can be a costly surprise."
   - Post-Promo APR tooltip: "The interest rate that applies after the promotional period ends. If deferred interest is enabled and you have a remaining balance, this rate was also accruing from day one."

3. `promo_cashback.html` -- 4 fields: APR, Loan Term, Cash-Back Amount, Down Payment.
   - APR, Loan Term, Down Payment: Same tooltips as traditional.
   - Cash-Back Amount tooltip: "A rebate you receive after the purchase, reducing your effective cost. For example, a $500 cash-back on a $10,000 purchase means your net cost is $9,500, though you still pay interest on the full $10,000."

4. `promo_price.html` -- 4 fields: Discounted Price, APR, Loan Term, Down Payment.
   - Discounted Price tooltip: "A reduced purchase price offered with this financing option. Unlike cash-back, you finance the lower amount. A $9,000 discounted price on a $10,000 item means you pay interest on $9,000."
   - APR, Loan Term, Down Payment: Same tooltips as traditional.

5. `custom.html` -- 3 fields with tooltips: Effective APR, Term, Upfront Cash. Description gets no tooltip.
   - Effective APR tooltip: "The combined annual cost rate including all fees and charges. Use this for financing options that don't fit standard categories -- enter the all-in annual rate."
   - Term tooltip: "Number of months for this financing arrangement."
   - Upfront Cash tooltip: "Total cash required at the time of purchase, including down payments, fees, or deposits."

6. `cash.html` -- Currently just a `<p>` tag. Add a small ? button inline with popover: "Paying the full purchase price upfront means no interest costs, but you lose the opportunity to invest that money elsewhere. The opportunity cost calculation shows what that invested money could have earned."

**Result templates:**

7. `breakdown_table.html`:
   - In the row loop, wrap `{{ row.label }}` in `.label-with-tip` div with ? button after the label text in the `<td class="row-label">` cell.
   - Use `tip-metric-{{ loop.index0 }}` for popover IDs.
   - Popover content from `{{ row.tooltip }}` (added to breakdown row dicts in 09-01).
   - Only show the ? button and popover div if `row.tooltip` is truthy: `{% if row.tooltip %}...{% endif %}`.

8. `recommendation.html`:
   - Add ? button next to "True Total Cost:" text in the `.winner-cost` paragraph.
   - Popover content: "True Total Cost is the bottom line -- the total amount this option really costs you after accounting for interest, opportunity cost of your money, inflation's effect on future payments, and any tax savings. Lower is better."
   - Use static ID `tip-rec-true-total-cost`.

Draft all tooltip copy as specified above. Each tooltip: 2-3 sentences covering definition, why it matters, and a concrete example with dollar amounts where helpful. Write in plain English.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run python -c "
from fathom import create_app
app = create_app()
client = app.test_client()
resp = client.get('/')
assert resp.status_code == 200, f'Index returned {resp.status_code}'
html = resp.data.decode()
# Check option field tooltips rendered
assert 'tip-apr-' in html, 'APR tooltip missing from option fields'
assert 'label-with-tip' in html, 'label-with-tip wrapper missing'
print('Option field templates render OK')

# Submit form to get results and check result tooltips
resp2 = client.post('/', data={
    'purchase_price': '10000',
    'option_count': '2',
    'option-0-type': 'traditional',
    'option-0-name': 'Test Loan',
    'option-0-apr': '6',
    'option-0-term': '24',
    'option-0-down_payment': '0',
    'option-1-type': 'cash',
    'option-1-name': 'Cash',
    'return_rate_preset': 'custom',
    'custom_return_rate': '7',
    'inflation_rate': '3',
})
assert resp2.status_code == 200, f'POST returned {resp2.status_code}'
html2 = resp2.data.decode()
assert 'tip-metric-' in html2, 'Breakdown metric tooltips missing'
assert 'tip-rec-true-total-cost' in html2, 'Recommendation tooltip missing'
print('Result templates render OK')
print('All template verifications passed')
" && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>Every form field label across all 6 option type templates has a ? button opening a popover with plain-English explanation. Breakdown table rows have ? buttons with explanations from row.tooltip. Recommendation card has ? next to True Total Cost. Cash option has inline ? button. All popover IDs are globally unique (field-name + opt.idx for options, static names for globals/results). No lint errors. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run ruff check . && uv run ruff format --check .` -- zero lint/format errors
- `uv run pytest tests/ -x -q` -- all existing tests pass
- Flask test client GET `/` returns 200 with tooltip buttons in all form fields
- Flask test client POST `/` returns 200 with tooltip buttons in breakdown table and recommendation
- Income ranges in bracket table use `|comma` Jinja filter (no pre-formatted strings from Python)
</verification>

<success_criteria>
- All form field labels have a ? button that opens a popover with explanation text (TIPS-01)
- All result metric labels in breakdown table have ? buttons with explanations (TIPS-02)
- Recommendation card True Total Cost has a ? button (TIPS-02)
- All ? buttons are keyboard-focusable, Escape dismisses popovers, content persists until dismissed (TIPS-03)
- Tax bracket details widget expands below tax rate field showing 7 rates for Single and MFJ (TAX-01, TAX-02)
- Income ranges formatted using |comma Jinja filter per user decision (TAX-02)
- Clicking a bracket row fills the tax rate input (TAX-01)
- Zero new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/09-tooltips-and-tax-guidance/09-02-SUMMARY.md`
</output>
