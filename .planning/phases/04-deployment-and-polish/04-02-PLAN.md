---
phase: 04-deployment-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
autonomous: true
requirements:
  - TECH-07
  - TECH-08

must_haves:
  truths:
    - "Project has an MIT license file at the root"
    - "README explains what Fathom is and how to run it"
    - "README documents Docker self-hosting with build and run commands"
    - "README documents bare-metal installation via uv"
    - "README has an environment variable reference table"
    - "README describes the architecture at a high level"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text with current year and author"
      contains: "MIT License"
    - path: "README.md"
      provides: "Comprehensive project documentation"
      min_lines: 80
  key_links: []
---

<objective>
Project documentation and licensing: MIT license file and comprehensive README with self-hosting instructions, env var reference, architecture overview, and development setup.

Purpose: Make the project ready for open-source distribution with clear instructions for self-hosting.
Output: LICENSE, README.md
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-and-polish/04-CONTEXT.md

<interfaces>
<!-- README needs to document these commands and config values -->

From pyproject.toml:
```toml
[project.scripts]
fathom = "fathom:main"
```

Config env vars (from CONTEXT.md decisions):
- FATHOM_SECRET_KEY, FATHOM_HOST, FATHOM_PORT, FATHOM_DEBUG
- FATHOM_WORKERS, FATHOM_DEFAULT_RETURN_RATE, FATHOM_DEFAULT_INFLATION_RATE, FATHOM_DEFAULT_TAX_RATE

Docker commands (no docker-compose):
- `docker build -t fathom .`
- `docker run -p 5000:5000 fathom`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>
Create the LICENSE file at project root with standard MIT license text.
- Year: 2026
- Copyright holder: read from pyproject.toml authors field (Kris Knigga)
- Use the exact standard MIT license text from https://opensource.org/licenses/MIT
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && test -s LICENSE && head -1 LICENSE | grep -q "MIT"</automated>
  </verify>
  <done>MIT LICENSE file exists at project root with correct year and author</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive README.md</name>
  <files>README.md</files>
  <action>
Write README.md with the following sections (technical, concise tone, developer audience, no screenshots per user decision):

1. **Title and tagline**: "# Fathom" followed by one-line description: "A financing options analyzer that compares the true total cost of different financing options, accounting for opportunity costs, inflation, and tax implications."

2. **Features** (brief bullet list):
   - Compare up to 4 financing options side-by-side
   - 6 option types: Cash, Traditional Loan, 0% Promo, Cash-Back, Price Reduction, Custom
   - True Total Cost calculation including opportunity cost, inflation, and tax savings
   - Interactive SVG charts with accessible data tables
   - Server-side rendering with HTMX for dynamic updates
   - Fully self-contained (no CDN dependencies, no database)

3. **Quick Start** with two subsections:
   a. **Docker** (recommended):
      ```bash
      docker build -t fathom .
      docker run -p 5000:5000 fathom
      ```
      Then visit http://localhost:5000
   b. **Bare metal (uv)**:
      ```bash
      uv sync
      uv run fathom
      ```
      Then visit http://localhost:5000

4. **Configuration**: Intro sentence explaining all config is via FATHOM_-prefixed env vars with sensible defaults. Table with columns: Variable, Default, Description. List all 8 env vars:
   - FATHOM_SECRET_KEY | fathom-dev-key | Flask session secret key
   - FATHOM_HOST | 0.0.0.0 | Server bind address
   - FATHOM_PORT | 5000 | Server port
   - FATHOM_DEBUG | false | Enable Flask debug mode
   - FATHOM_WORKERS | 2 | Gunicorn worker processes
   - FATHOM_DEFAULT_RETURN_RATE | 0.07 | Default investment return rate (7%)
   - FATHOM_DEFAULT_INFLATION_RATE | 0.03 | Default inflation rate (3%)
   - FATHOM_DEFAULT_TAX_RATE | 0.22 | Default marginal tax rate (22%)
   Add note: "You can also use a `.env` file in the project root."

5. **Architecture**: Brief overview (3-5 sentences) covering: Flask SSR app, Jinja2 templates with Pico CSS, HTMX for partial page updates, server-rendered SVG charts, stateless (no database), all financial math in Python using Decimal arithmetic. Include project structure tree:
   ```
   src/fathom/
     __init__.py      # Entry point
     app.py           # Flask app factory
     config.py        # Environment configuration
     routes.py        # HTTP route handlers
     engine.py        # Calculation engine orchestrator
     models.py        # Domain models (FinancingOption, etc.)
     forms.py         # Form parsing and validation
     results.py       # Results analysis for display
     charts.py        # SVG chart data preparation
     amortization.py  # Loan amortization calculations
     opportunity.py   # Opportunity cost calculations
     inflation.py     # Inflation adjustment
     tax.py           # Tax savings calculations
     caveats.py       # Warning/caveat generation
     templates/       # Jinja2 HTML templates
     static/          # CSS and vendor assets
   ```

6. **Development**:
   ```bash
   uv sync                    # Install dependencies
   uv run fathom              # Run dev server
   uv run pytest              # Run tests
   uv run ruff check .        # Lint
   uv run ruff format .       # Format
   uv run ty check            # Type check (ty)
   uv run pyrefly check       # Type check (pyrefly)
   uv run prek run            # Pre-commit hooks
   ```

7. **License**: "MIT -- see [LICENSE](LICENSE)."
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && test -s README.md && grep -q "Quick Start" README.md && grep -q "FATHOM_" README.md && grep -q "docker" README.md && grep -q "uv" README.md && wc -l README.md | awk '{if ($1 >= 80) print "OK"; else print "FAIL: too short"}'</automated>
  </verify>
  <done>README.md has all required sections: features, Docker + bare-metal quick start, env var reference table, architecture overview, development commands, license reference</done>
</task>

</tasks>

<verification>
- `test -s LICENSE` -- LICENSE file exists and is non-empty
- `grep -q "MIT" LICENSE` -- Contains MIT license
- `test -s README.md` -- README exists and is non-empty
- `grep -q "docker build" README.md` -- Docker instructions present
- `grep -q "uv run fathom" README.md` -- Bare-metal instructions present
- `grep -q "FATHOM_SECRET_KEY" README.md` -- Env var reference present
</verification>

<success_criteria>
- MIT LICENSE file at project root with 2026 date and correct author
- README.md with all 7 sections covering what/why/how
- Docker and uv installation methods both documented
- All 8 FATHOM_ env vars documented in reference table
- Architecture section shows project structure
- Development section lists all quality commands
- No screenshots, no dark mode, no print CSS (deferred items excluded)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-and-polish/04-02-SUMMARY.md`
</output>
