---
phase: 04-deployment-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fathom/config.py
  - src/fathom/app.py
  - src/fathom/__init__.py
  - src/fathom/routes.py
  - src/fathom/templates/base.html
  - src/fathom/static/vendor/pico.min.css
  - src/fathom/static/vendor/htmx.min.js
  - pyproject.toml
  - uv.lock
  - Dockerfile
  - .dockerignore
  - tests/test_config.py
  - tests/test_performance.py
autonomous: true
requirements:
  - TECH-05
  - TECH-06

must_haves:
  truths:
    - "Application loads configuration from FATHOM_-prefixed environment variables"
    - "Invalid env var values crash on startup with clear validation error"
    - "App runs with zero configuration using sensible defaults"
    - "Default financial rates (return, inflation, tax) are configurable via env vars and affect form defaults"
    - "Pico CSS and HTMX load from local vendor files, not CDN"
    - "Dockerfile builds and produces a runnable container image"
  artifacts:
    - path: "src/fathom/config.py"
      provides: "Settings class with FATHOM_ prefix, typed validation, defaults"
      contains: "class Settings"
    - path: "src/fathom/static/vendor/pico.min.css"
      provides: "Bundled Pico CSS for air-gapped operation"
      min_lines: 1
    - path: "src/fathom/static/vendor/htmx.min.js"
      provides: "Bundled HTMX JS for air-gapped operation"
      min_lines: 1
    - path: "Dockerfile"
      provides: "Multi-stage Docker build with gunicorn entrypoint"
      contains: "gunicorn"
    - path: ".dockerignore"
      provides: "Excludes non-production files from Docker context"
      min_lines: 3
    - path: "tests/test_config.py"
      provides: "Tests for Settings defaults, env var override, invalid values"
      min_lines: 20
    - path: "tests/test_performance.py"
      provides: "Response time assertion under 300ms"
      min_lines: 10
  key_links:
    - from: "src/fathom/app.py"
      to: "src/fathom/config.py"
      via: "Settings import and create_app parameter"
      pattern: "from fathom\\.config import Settings"
    - from: "src/fathom/routes.py"
      to: "app.config['FATHOM_SETTINGS']"
      via: "current_app.config for default rates"
      pattern: "FATHOM_SETTINGS"
    - from: "src/fathom/templates/base.html"
      to: "src/fathom/static/vendor/"
      via: "url_for static references"
      pattern: "vendor/pico\\.min\\.css"
    - from: "Dockerfile"
      to: "src/fathom/app.py"
      via: "gunicorn CMD referencing create_app"
      pattern: "fathom\\.app:create_app"
---

<objective>
Production infrastructure: typed environment configuration via pydantic-settings, bundled static assets for air-gapped operation, multi-stage Dockerfile with gunicorn, and tests covering config validation and performance.

Purpose: Transform the development Flask app into a production-ready, self-hostable container with configurable defaults.
Output: config.py, vendor assets, Dockerfile, .dockerignore, test_config.py, test_performance.py
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-and-polish/04-RESEARCH.md

<interfaces>
<!-- Current app factory and entry point that must be modified -->

From src/fathom/app.py:
```python
def create_app() -> Flask:
    app = Flask(__name__, template_folder="templates", static_folder="static")
    app.config["SECRET_KEY"] = os.environ.get("SECRET_KEY", "fathom-dev-key")
    from fathom.routes import bp
    app.register_blueprint(bp)
    return app
```

From src/fathom/__init__.py:
```python
def main() -> None:
    from fathom.app import create_app
    app = create_app()
    app.run(debug=True)
```

From src/fathom/routes.py (index route with hardcoded defaults):
```python
@bp.route("/")
def index() -> str:
    return render_template(
        "index.html",
        purchase_price="",
        options=_build_default_options(),
        option_types=_build_option_types(),
        settings={
            "return_preset": "0.07",
            "return_rate_custom": "",
            "inflation_enabled": False,
            "inflation_rate": "3",
            "tax_enabled": False,
            "tax_rate": "22",
        },
        errors={},
    )
```

From src/fathom/templates/base.html (CDN links to replace):
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
<script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>
```

From pyproject.toml (current dependencies):
```toml
dependencies = ["flask>=3.1.3"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, create config.py, bundle vendor assets, wire config through app</name>
  <files>pyproject.toml, uv.lock, src/fathom/config.py, src/fathom/app.py, src/fathom/__init__.py, src/fathom/routes.py, src/fathom/templates/base.html, src/fathom/static/vendor/pico.min.css, src/fathom/static/vendor/htmx.min.js</files>
  <action>
1. Add production dependencies: `uv add gunicorn pydantic-settings python-dotenv`
2. Update pyproject.toml metadata: change description from "Add your description here" to "A financing options analyzer that compares the true total cost of different financing options". Add classifiers: Development Status (4 - Beta), Framework (Flask), License (MIT), Programming Language (Python :: 3.14), Topic (Office/Business :: Financial).
3. Create `src/fathom/config.py` with a Settings class using pydantic-settings BaseSettings:
   - `model_config = SettingsConfigDict(env_prefix="FATHOM_", env_file=".env", env_file_encoding="utf-8")`
   - Fields with defaults: secret_key (str, "fathom-dev-key"), host (str, "0.0.0.0"), port (int, 5000), debug (bool, False), workers (int, 2), default_return_rate (float, 0.07), default_inflation_rate (float, 0.03), default_tax_rate (float, 0.22)
   - Add proper docstring for the class and module
4. Update `src/fathom/app.py`:
   - Change `create_app()` signature to `create_app(settings: Settings | None = None) -> Flask`
   - If settings is None, instantiate `Settings()`
   - Replace `os.environ.get("SECRET_KEY", ...)` with `settings.secret_key`
   - Store settings: `app.config["FATHOM_SETTINGS"] = settings`
   - Remove `import os` (no longer needed)
5. Update `src/fathom/__init__.py`:
   - Import Settings, instantiate it, pass to create_app
   - If `settings.debug` is True, run `app.run(host=settings.host, port=settings.port, debug=True)`
   - If debug is False, still run `app.run(host=settings.host, port=settings.port)` -- production users use gunicorn directly via CLI or Docker, main() stays as dev convenience
6. Update `src/fathom/routes.py` index route:
   - Import `current_app` from flask
   - Read settings from `current_app.config["FATHOM_SETTINGS"]` inside the index() function
   - Replace hardcoded "0.07" with `str(settings.default_return_rate)`, "3" with `str(int(settings.default_inflation_rate * 100))`, "22" with `str(int(settings.default_tax_rate * 100))`
   - This ensures FATHOM_DEFAULT_RETURN_RATE etc. actually affect form defaults
7. Download vendor assets:
   - `mkdir -p src/fathom/static/vendor`
   - `curl -o src/fathom/static/vendor/pico.min.css "https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"`
   - `curl -o src/fathom/static/vendor/htmx.min.js "https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"`
8. Update `src/fathom/templates/base.html`:
   - Replace CDN Pico link with `{{ url_for('static', filename='vendor/pico.min.css') }}`
   - Replace CDN HTMX script with `{{ url_for('static', filename='vendor/htmx.min.js') }}`
9. Run `uv run ruff check . && uv run ruff format --check . && uv run pytest -x -q` to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run ruff check . && uv run ruff format --check . && uv run pytest -x -q</automated>
  </verify>
  <done>pydantic-settings config wired through app factory, vendor assets bundled locally, CDN links replaced, all existing tests pass, linting clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dockerfile, .dockerignore, config tests, and performance test</name>
  <files>Dockerfile, .dockerignore, tests/test_config.py, tests/test_performance.py</files>
  <behavior>
    - test_config: Settings() returns sensible defaults (port=5000, debug=False, workers=2, etc.)
    - test_config: Settings with FATHOM_PORT env var override returns the overridden value
    - test_config: Settings with FATHOM_SECRET_KEY env var override returns custom key
    - test_config: Settings with invalid FATHOM_PORT (e.g., "abc") raises ValidationError
    - test_config: Settings default_return_rate default is 0.07
    - test_config: create_app() with custom Settings uses the provided settings
    - test_config: create_app() stores settings in app.config["FATHOM_SETTINGS"]
    - test_performance: POST /compare with valid form data returns 200 within 300ms (using Flask test client timing)
  </behavior>
  <action>
1. Create `tests/test_config.py`:
   - Test Settings default values (all 8 fields)
   - Test env var override using monkeypatch (set FATHOM_PORT=8080, verify settings.port == 8080)
   - Test FATHOM_SECRET_KEY override
   - Test invalid env var (FATHOM_PORT=abc) raises pydantic ValidationError
   - Test create_app(Settings(...)) passes settings through to app.config
   - Test app.config["FATHOM_SETTINGS"] is accessible
   - Use monkeypatch.setenv for env var tests, monkeypatch.delenv with raising=False to clean env

2. Create `tests/test_performance.py`:
   - Import create_app, time
   - Create test client, POST /compare with a valid form payload (purchase price $10000, two options: cash and traditional loan with APR 5.99, term 36, down payment 1000)
   - Measure wall-clock time for the request
   - Assert response.status_code == 200
   - Assert elapsed time < 0.3 seconds (300ms PERF-01 requirement)
   - Run 5 iterations to get average, assert average < 0.3

3. Create `Dockerfile` (multi-stage, per user decision):
   ```
   # Stage 1: Build
   FROM python:3.14-slim AS builder
   RUN pip install uv
   WORKDIR /app
   COPY pyproject.toml uv.lock ./
   RUN uv sync --frozen --no-dev
   COPY src/ src/

   # Stage 2: Run
   FROM python:3.14-slim
   WORKDIR /app
   COPY --from=builder /app/.venv /app/.venv
   COPY --from=builder /app/src /app/src
   ENV PATH="/app/.venv/bin:$PATH"
   EXPOSE 5000
   CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "fathom.app:create_app()"]
   ```
   Use shell form for CMD if configurable workers needed: `CMD gunicorn --bind 0.0.0.0:${FATHOM_PORT:-5000} --workers ${FATHOM_WORKERS:-2} 'fathom.app:create_app()'`

4. Create `.dockerignore`:
   ```
   .planning/
   tests/
   .git/
   .github/
   .venv/
   __pycache__/
   *.pyc
   .ruff_cache/
   .pytest_cache/
   .env
   docs/
   *.md
   !README.md
   .pre-commit-config.yaml
   .prek.yaml
   ```

5. Run full test suite including new tests.
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && uv run pytest tests/test_config.py tests/test_performance.py -x -v && uv run ruff check tests/test_config.py tests/test_performance.py && uv run ruff format --check tests/test_config.py tests/test_performance.py</automated>
  </verify>
  <done>Dockerfile builds multi-stage image with gunicorn, .dockerignore excludes dev files, config tests verify defaults/overrides/validation, performance test confirms sub-300ms response</done>
</task>

</tasks>

<verification>
- `uv run pytest -x -q` -- all tests pass including new config and performance tests
- `uv run ruff check . && uv run ruff format --check .` -- linting clean
- `python -c "from fathom.config import Settings; s = Settings(); print(s.port, s.debug)"` -- prints "5000 False"
- Vendor files exist: `test -s src/fathom/static/vendor/pico.min.css && test -s src/fathom/static/vendor/htmx.min.js`
- Dockerfile exists: `test -s Dockerfile`
</verification>

<success_criteria>
- Settings class loads FATHOM_-prefixed env vars with typed validation
- create_app accepts optional Settings parameter
- Form defaults read from Settings (not hardcoded)
- Pico CSS and HTMX served from local vendor files
- Dockerfile multi-stage build with gunicorn CMD
- Config tests cover defaults, overrides, and invalid values
- Performance test confirms sub-300ms response time
- All quality gates pass (ruff, pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-and-polish/04-01-SUMMARY.md`
</output>
