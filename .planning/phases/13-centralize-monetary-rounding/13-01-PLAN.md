---
phase: 13-centralize-monetary-rounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fathom/money.py
  - src/fathom/amortization.py
  - src/fathom/opportunity.py
  - src/fathom/inflation.py
  - src/fathom/caveats.py
  - src/fathom/tax.py
  - src/fathom/engine.py
autonomous: true
requirements:
  - ENG-03

must_haves:
  truths:
    - "quantize_money() is defined in exactly one file (money.py)"
    - "CENTS constant is defined in exactly one file (money.py)"
    - "All 283+ existing tests pass with zero behavior change"
    - "All quality gates (ruff, ty, pyrefly) pass clean"
  artifacts:
    - path: "src/fathom/money.py"
      provides: "Canonical monetary rounding utility"
      exports: ["quantize_money", "CENTS"]
      contains: "def quantize_money"
  key_links:
    - from: "src/fathom/amortization.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
    - from: "src/fathom/opportunity.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
    - from: "src/fathom/inflation.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
    - from: "src/fathom/caveats.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
    - from: "src/fathom/tax.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
    - from: "src/fathom/engine.py"
      to: "src/fathom/money.py"
      via: "from fathom.money import quantize_money"
      pattern: "from fathom\\.money import quantize_money"
---

<objective>
Centralize all monetary rounding into a single `src/fathom/money.py` module, replacing 5 duplicate `quantize_money()` + `CENTS` definitions.

Purpose: Eliminate drift risk from 5 identical copies of the same rounding logic across calculation modules.
Output: One canonical `money.py` module; 6 files updated to import from it; zero behavior change confirmed by full test suite.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-centralize-monetary-rounding/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create money.py canonical module</name>
  <files>src/fathom/money.py</files>
  <action>
Create new file `src/fathom/money.py` with:

1. Module docstring: "Centralized monetary rounding utilities." plus a sentence about consistent two-decimal-place precision.
2. Import `Decimal` from `decimal`.
3. Define `CENTS = Decimal("0.01")` module-level constant.
4. Define `quantize_money(value: Decimal) -> Decimal` function with docstring, returning `value.quantize(CENTS)`.

This is identical to the existing definitions. Do NOT add rounding mode parameters or any new functionality. Do NOT import from any fathom module (stdlib only to avoid circular imports).
  </action>
  <verify>
    <automated>python -c "from fathom.money import quantize_money, CENTS; from decimal import Decimal; assert quantize_money(Decimal('1.005')) == Decimal('1.00'); print('OK')"</automated>
  </verify>
  <done>src/fathom/money.py exists with quantize_money() and CENTS exported, importable, and producing correct results</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate definitions with imports from money.py</name>
  <files>src/fathom/amortization.py, src/fathom/opportunity.py, src/fathom/inflation.py, src/fathom/caveats.py, src/fathom/tax.py, src/fathom/engine.py</files>
  <action>
Update 6 files to import from the new canonical module:

**In each of these 5 files** (amortization.py, opportunity.py, inflation.py, caveats.py, tax.py):
1. Remove the `CENTS = Decimal("0.01")` constant definition
2. Remove the `def quantize_money(value: Decimal) -> Decimal:` function definition (and its docstring/body)
3. Add `from fathom.money import quantize_money` to the imports section
4. If `Decimal` is no longer used directly in the file after removing CENTS, remove the `from decimal import Decimal` import too. Check each file individually -- some use `Decimal` elsewhere.

**In engine.py:**
1. Change `from fathom.amortization import amortization_schedule, quantize_money` to `from fathom.amortization import amortization_schedule`
2. Add `from fathom.money import quantize_money` as a separate import

After all changes, run `uv run ruff check --fix .` to auto-sort imports, then `uv run ruff format .` to format.

**Verification greps (must pass):**
- `grep -rn "def quantize_money" src/fathom/` must show exactly 1 match in money.py
- `grep -rn "CENTS = Decimal" src/fathom/` must show exactly 1 match in money.py
  </action>
  <verify>
    <automated>cd /home/kris/git/fathom && grep -c "def quantize_money" src/fathom/money.py | grep -q "1" && test "$(grep -rn 'def quantize_money' src/fathom/ | wc -l)" -eq 1 && test "$(grep -rn 'CENTS = Decimal' src/fathom/ | wc -l)" -eq 1 && uv run pytest tests/ -x -q && uv run ruff check . && uv run ruff format --check . && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>All 5 duplicate definitions removed. All 6 files import from money.py. grep confirms exactly 1 definition. All 283+ tests pass. Ruff clean.</done>
</task>

</tasks>

<verification>
Run full quality gate suite after both tasks complete:

1. `grep -rn "def quantize_money" src/fathom/` -- exactly 1 match (money.py)
2. `grep -rn "CENTS = Decimal" src/fathom/` -- exactly 1 match (money.py)
3. `uv run pytest tests/` -- all tests pass
4. `uv run ruff check .` -- clean
5. `uv run ruff format --check .` -- clean
6. `uv run ty check` -- clean
7. `uv run pyrefly check` -- clean
</verification>

<success_criteria>
- src/fathom/money.py exists as the single canonical source for quantize_money() and CENTS
- Zero other files define quantize_money() or CENTS
- All 6 consumer files import from fathom.money
- All existing tests pass with zero behavior change
- All quality gates pass clean with no new inline suppressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-centralize-monetary-rounding/13-01-SUMMARY.md`
</output>
